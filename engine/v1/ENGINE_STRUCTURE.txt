engine/
  v1/
    Bootup.js // Initializes engine boot sequence and startup tasks

    core/
      config.js // Global engine configuration values (and settings application)
      ini.js // INI-style config parsing/initialization helpers (creates a global ENGINE to export to the game)
      meta.js // Metadata utilities (versioning, manifest helpers)
      splash.js // Splash screen management
      validate.js // Validation utilities for configs/assets

    builder/
      NewBoss.js // Builder for creating boss entities
      NewEntity.js // Builder for generic entity creation (anything that supports being animated)
      NewLevel.js // Builder for creating levels from incoming data (uses most other builders)
      NewObject.js // Builder of 3D objects (mainly to be used in other builders)
      NewObstacle.js // Builder of obstacles
      NewTexture.js // Builder of 2D and 3D textures for 3D objects
      NewUI.js // Builder of UI components
      templates/
        characters.json // Character template definitions for builder
        enemies.json // Enemy template definitions
        obstacles.json // Obstacle template definitions
        projectiles.json // Projectile template definitions

    handlers/
      Controls.js // Handles inputs from ui/level cache or sends inputs to game (input manager)
      Cutscene.js // Cutscene orchestration handler (cutscene manager)
      Render.js // Displays everything to the game (end of all visual pipelines)
      Sound.js // Sound manager and helpers
      UI.js // UI handler (UI state manager)

      game/
        Animation.js // Level entity animation handler
        Boss.js // Boss behavior handler
        Camera.js // Camera management for levels
        Collectible.js // Collectible item behavior
        Enemy.js // Enemy behavior handler
        Level.js // Level lifecycle and orchestration (entire level state manager)
        Physics.js // Game physics glue for handlers (physics pipeline manager)
        Texture.js // Texture and sprite loading/management for game objects

      menu/
        Splash.js // Manages splash screens on game startup
        LoadScreen.js // Loading screen logic
        Credits.js // Credits screen UI/behavior

    cutscene/
      Actors.js // Cutscene actor definitions, manager and helpers
      Animation.js // Cutscene-specific animation handler
      AudioSync.js // Sync audio with cutscene timelines (make sure audio plays at right time and speed)
      Scene.js // Cutscene scene management

    math/
      Curves.js // Curve utilities (e.g., bezier helpers)
      Forces.js // Force calculation helpers
      Vector3.js // 3D vector math utilities

    physics/
      Buoyancy.js // Buoyancy force calculations
      Collision.js // Collision detection utilities
      Correction.js // Collision correction/response helpers (e.g. adjusting player angle on slopes)
      Current.js // Movement adjustments while in underwater currents
      Gravity.js // Gravity helpers
      Resistance.js // Air/Water Resistance/drag computations

    player/
      Abilities.js // Player ability definitions, handler and logic
      Animation.js // Player animation controller (state manager)
      Master.js // Player master controller (player state manager)
      Model.js // Player model/data representation (uses builder for player model)
      Movement.js // Movement handler for player
      characters.json // Player character presets

    assets/
      engine/
        carlNetEngine.png // Engine branding/art
        carlNetEngine.mp3 // Engine audio/branding
      carlStudios/
        splat.mp3 // studio effect
        sloppyCarl.png // studio art
        sloppyCarl.mp3 // studio audio
      characters/
        carl/
          carl.png // Carl character sprite
          carl_wave.png // Carl wave sprite

    testGame/
      main.js // Central test game logic
      output.html // Test game visual output
      menus/
        ui.js // UI logic for testGame
        ui.json // UI payloads
      audio/
        titleTheme.mp3 // audio assets for testGame
        levelTheme.mp3
        creditsTheme.mp3
        bossTheme.mp3
      cutscene/
        cutscene.js // Cutscene logic for testGame
        cutscene.json // Cutscene payloads
        rendered/
          Opening.mp4 // Rendered opening cutscene video
      levels/
        levels.json // Level payloads
        level.js // Level logic for testGame
        level1.png // Level artwork for level select
        level2.parsing

Notes:
- Builders create 2D HTML elements and 3D WebGL Objects and are called by state managers.
  - Textures.js pastes 2D textures onto 3D objects for detail and creates 3D textures (using NewObject.js) for depth (grass, pebbles, etc)
- Helpers are state managers and helper functions to simplify pipeline. Most can be called from anywhere.
- Render.js is the only file allowed to display visual elements to the game.