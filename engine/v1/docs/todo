This file contains current priorities.

Add new characters to test multiple types of characters with intuitive physics
- Jeff (in progress)
- Latina
COMBAT REWORK:
- Projectile Functionality
- Damage and destroy objects
- Custom object destruction results & configurable particles
- Entity damage
- A seamless system of hit detection > damage/other effects
- Configured SFX
- "Weapons" & Attacks functionality
- Fix normalizeAttacks in core/normalize.js: the .map callback has no return, so every entity's attacks array is the right length and entirely undefined
Option to block scatter spawn on top of decals
- Add spikes to Carl model
Entity AI:
- Default behaviours (not mutually exclusive):
  - Chase
  - Search
  - Idle
  - Wander
  - Guard
  - Attack
  - Defend
- Custom behaviours: Game attaches handlers OR events.
Player Abilities:
- Pretty much have to be custom handlers. Optional use, game can choose to apply effects manually.
- Maybe some standards? Like double jump? Boosting?
Ledge balancing: partial rotation at an edge already reads as leaning — build it into an almost-falling state.
Rework Collectible system into collections with built-in handling support.
Build new trigger types:
- Level Clear
- Custom
- Teleport
Add new Terrain, Obstacle and Entity Templates.
- Think of a list with Claude
- Sonic-style Loop (Terrain)
- Half-Pipe (Terrain)
- City Rubble (Obstacle)
- Basketball Hoop (Obstacle)
- Shrine (Terrain)
Build new test level (prompt in notepad).
Build Load Screen functionality.
- Background Image support
- HTML Element support
- Music Support
Add aliases to canonSchemas.json
Performance Enhancements (DRYAD):
- Must abide simulation distance:
  - Any animations. From textures to entities.
  - Rendering instances. We must check whether skipping rendering based on distance would be worthwhile
    - If yes, then fade them in/out rather than allowing pop-in
  - Particle logic.
  - Advanced physics application.
  - Runtime instance spawning. Any instance that's being spawned too far, gets enqueued until the player gets close enough.
Per-entity render flags, set during the SimDistance check collision already runs:
- render.animatedTexture: texture gate ORs its owners' flags instead of testing every owner AABB per frame.
- render.model: skip rendering distant entities entirely. Reaches into the decal and translucent passes, so it's its own pass.
- Force the SimDistance check even when an entity's collision flag is off.
- Entity-owned textures only. Large static owners (sea-surface, terrain, obstacles) keep point-to-AABB.
- Multi-owner still needs the OR. energy-core resolves two owners at different distances.


Additions to Level API:
- Entities:
  - GetState(entityId)
  - GetAll
  - Animate(entityId, ...)
  - Kill(entityId)
  - Delete(entityId)
  - Heal(entityId, amount)
  - SetPhysics(entityId, ...)
  - SetMeta(entityId, ...)
  - SetPosition(entityId)
  - SetScale(entityId)
  - Damage(objectType, id, amount)
  - SetAction(entityId, ...)
  - SetNpcBehavior(entityId, ...)
  - NpcAccessories
- Player:
  - Animate
  - CharacterSwitch
  - SetPosition
  - SetScale
  - SetMeta
  - SetAction
  - Kill
  - Heal
  - Damage
  - Launch(velocity) — sets velocity and the `launched` flag together, so games can make the player lose their footing.
  - Accessories
  - GetState
- Spawn:
  - Terrain
  - Obstacle
  - Entity
  - Trigger
- Camera:
  - Switch
  - SetPosition
  - GetPosition
  - Snapshot
- Trigger(triggerId)
- Do:
  - 
- GetSnapshot


Add full documentation API so devs can understand how to work the engine.


Create a level interpreter that reads level json data and returns 2D representations of the level.
- The output can be ASCII/data (for AI)
- The output can be JPEGs
- Output can be multiple. Things stacked on top of each other are hard to define, so each piece of output should reflect a height region based on the height differences in objects (aka dynamic)


Allow special rotational control so Carl can rotate in water with Q and E
- Line up to things you want to attach to.
- A and D controls change direction.


Version Goals:
- 0.32: Main Update Released [FINISHED]
- 0.33: Decal Rework [PATCHING]
  1. Pre-load Simulator Cache with player-characters and all templates. [DONE]
  2. Jump Window Expansion (ground probe so early presses can register early; tiny window after losing ground contact) [DONE]
    - Make rendering obey FrameRate. [DONE]
  3. Sticky Terrain: [DONE]
    - Fix bug where you clip out of bounds in voids. [DONE]
    - Add a "sticky" field to terrain. The player's grounding is no longer tied to a minimum speed while grounded on this part. [DONE]
  4. Camera & Rendering Versatility
    - Offset the camera at incline changes so it remains vertically stationary during loops. Ensure it doesn't influence movement.
    - Backface Culling
    - Toon-switch for outlines.
  5. DEFERRED Performance Enhancements [PENDING]
    - Move Render.js Shaders to new Shaders.js module.
    - Move Object.js shape functions to new NewPrimitive.js module.
  6. Todo Performance Enhancements [PENDING]
- 0.34: Entity AI
  1. Add optional Q/W rotation controls.
  2. Add custom player control option.
  3. Add optional Swimming & Flying states.
  4. Add player/characters.json animation authoring support.
  x. Bug fixes.
- 0.35: Combat Rework
  x. Bug fixes.
- 0.36: Collectible Rework
  1. Bug fixes.
  2. Increase game-side control in sub-systems.
  3. Load Screen functionality.
- 0.37: Moving Terrain & Obstacles.
- 0.38: DEFERRED Backlog Cleanup
  1. Optionally block scatter on decals.
  2. Add scatter support to voids.
  3. Block scatter inside objects.
    - Block generation if cheaper than post-generation block.
    - Mark for runtime culling for moving platforms.
  4. Add lighting.
  x. Debug & finish testGame level 2.
- 0.39: Add entity (player, npcs, bosses) accessories support.
  - Pre-built, but not rendered (or zero-scaled).
  - Accessories can be toggled individually through the API.
- 0.40: Populate Templates, Scatter & Textures.
- 0.41: Engine Cutscenes
  1. Add credits builder.
  2. Add dialogue system.
- 0.42: Add testGame boss level.
  x. Debugging.
- 1.0: Full API & Documentation
  - Add canonSchemas aliases.
  - Improve validation errors and warnings.
    - Errors only give the generic path, not the actual degenerate entry.
    - Errors don't disclose how many sibling or child entries are affected/lost.
  - Improve engine processes logging.
- 1.1: CarlNet Editor
  - Transform the Simulator App to a proper 3D editor.
    - It will be capable of adding, resizing and moving objects to build models.
    - This allows the building of entities, bosses, obstacles and terrain chunks.
- 1.2: Jeff, Latina & Custom Character Support