# Agent Log

Running log of notable engine-touching work — not custom agents alone. Valid entry
sources are ERA, DRYAD, ARGUS, RIGOR, ED, SAGE, and MAIN (the main/orchestrating
agent acting directly, without spawning a custom agent). SAGE is responsible for
appending an entry here after any of these produces a meaningful finding or change,
including the main agent's own passes. The weekly status task reads this file and
then empties it (back to this header) after summarizing — so entries only need to
cover "since the last weekly report."

## Entry format

`- [YYYY-MM-DD] AGENT: task — outcome (authorized actions taken, if any)`

Examples:

`- [2026-07-09] DRYAD: reviewed NewObject.js tube refactor — found 1 duplicated matrix-multiply, flagged only (no fix authorized)`
`- [2026-07-09] MAIN: applied a flagged low-severity dedup fix directly — extracted a shared helper, no agent spawned`

## Log

- [2026-08-23] MAIN: comment-style cleanup in [Texture.js](engine/v1/handlers/game/Texture.js) after the author flagged the module — no logic changed, `node --check` clean. Calibrated against the sibling handlers (`Animation.js`, `Camera.js`, `Enemy.js`, `Level.js`, `Simulator.js`) rather than trimming by feel, which located the boundary: those files pair a 4–5 line module header with short fragment comments inline, so Texture.js's header was **convention, not a violation**, and was left untouched — the inline comments were the whole of it. Nine rewritten: the three-line paragraph above `assignOwnerMeshes` collapsed to two fragments, the two-line comment in `createAnimationStateEntry` merged to one, and seven full-sentence comments cut to fragments (`bakeReachFraction` trailing, `sqDistanceToAabb`, the water fixture claim, the bake gate, the `toSurface`/lookahead branch, the two-surface blend precondition, the hold-until-lookahead return, and `isOwnerWithinReach`). Every one of these was authored during the pass-4 texture-gating work logged below, so the density was introduced in a single pass rather than accumulated.

- [2026-08-22] SAGE: research pass on terrain-scatter performance ahead of a scope, at the author's request; no code changed, findings collected in [PERFORMANCE_BRAINSTORM.md](engine/v1/docs/PERFORMANCE_BRAINSTORM.md) as a temporary discussion file. Two findings reframe the author's premises. **Scatter is never distance-culled** — instances are packed once into `STATIC_DRAW` buffers at level build and every blade in the level is submitted every frame with no distance or frustum test anywhere in the path ([Render.js:702-765](engine/v1/handlers/Render.js#L702-L765)), so folding scatter under SimDistance is not a rename of the dead `RenderDistance` but the introduction of scatter culling for the first time. **`RenderDistance` is confirmed dead** — one declaration at [config.js:95](engine/v1/core/config.js#L95), zero readers engine-wide, absent even from testGame's settings snapshot. Cost baseline measured from source: an 8-segment `complexity: "low"` cylinder is 50 verts / 32 tris, of which the two caps are 16 tris; grass is 5 such parts, so 250 verts / 160 tris per clump, and the caps are a buried bottom face and a 0.02-CNU top face that never resolves — an `openEnded` primitive option would halve grass geometry at every tier with no visible change and no new setting. Raised against the author's proposal: per-field `presets.low/medium/high` cannot express a crossed-plane variant, which is a different *part list*, not the same parts with a swapped primitive — tier-keyed part lists proposed instead. Also raised: a stored `"Low"` currently means *off*, so adding a `Disabled` tier needs a migration ruling; `TIER_LEVELS` is one shared list across all four tier settings and would need to go per-setting; `PERFORMANCE.Animations.Active` becomes the odd one out if off-ness moves into the tier value elsewhere; and a single SimDistance number would be driving four radii that want different scales. Texture-animation gating flagged as **not yet verifiable** — `UpdateTextureAnimation` is ungated ([Level.js:336](engine/v1/handlers/game/Level.js#L336)) but `handlers/game/Texture.js` was not read, and if state is keyed per generated-texture rather than per object, two objects sharing a texture cannot be gated independently. All decisions left TBD.
- [2026-08-23] MAIN: post-audit hardening before a second ARGUS run — three things. **Found and fixed a broken helper:** the comment-style cleanup logged above had rewritten `sqDistanceToAabb` in [Texture.js](engine/v1/handlers/game/Texture.js) into a one-liner referencing an undefined `point` when the parameter is `pos`, and calling `DotVector3` with one argument when it takes two. That is a `ReferenceError` on the first animated texture and would have blocked the ARGUS run outright. That entry's claim of "no logic changed" was inaccurate — the helper was rewritten, moved inside `isOwnerWithinReach` so it was reconstructed per texture per frame, and its imports swapped. Flagged rather than silently reverted, then corrected and hoisted back to module scope; since polished on disk to use `Vector3Sq`, which is the correct single-argument squared-length helper and better than the interim form. **Closed the walk-drift tech debt structurally**, per the author's ruling that a more robust connection was worth having even though the two walks worked while hand-synced: `ForEachTexturedMesh` in [NewTexture.js](engine/v1/builder/NewTexture.js) is now the one enumeration of texture-owning meshes, consumed by both `collectTextureUsage` and `assignOwnerMeshes`, so a future collection reaches both by construction. Verified behaviour-identical for registration point by point — `obstacle.mesh` is `parts[0]` and `collectMesh` is idempotent, the entity walk already used `model.parts` unconditionally, and owner resolution inheriting registration's `mode` filters is harmless since an unregistered texture cannot become an animation entry. **Applied the author's `Scatter.Amount` → `Scatter.Density` rename** across six files, and renamed `PERFORMANCE_AMOUNT_SCALE` → `PERFORMANCE_TIER_SCALE` unprompted, since a constant named AMOUNT feeding a key named Density is exactly the small incoherence flagged in the preceding tech-debt review. Residual sweep returned zero `Amount` references in engine or testGame source; `node --check` clean on all seven edited `.js` files and both JSON files parse. Prepared the handoff for a context compact by adding a "State at the second ARGUS run" section to [PERFORMANCE_BRAINSTORM.md](engine/v1/docs/PERFORMANCE_BRAINSTORM.md).
- [2026-08-23] ARGUS: browser-tested the four-pass performance patch, logical/behavioural scope only — **blocked by one fatal defect, but ruled out all four warned-about silent failure modes by measurement before hitting it.** The blocker: the game froze on frame one of any level with water. The browser kept painting (rAF at 77/s) but the update loop was dead — a player with action `Falling` and negative Y velocity did not move across a full second. Console carried `Uncaught TypeError: Cannot read properties of null (reading 'worldAabb')`. ARGUS traced the cause rather than reporting the symptom: it scanned all three walked collections for the `sea-surface` texture id across both `material.textureID` and `faceTextureGroups[].textureID`, got zero hits, then ran a full sceneGraph traversal that located it on `sceneGraph.waterVisual.top` (`water-top-120-80-9`) carrying a valid `worldAabb` — a coverage gap, not a matching bug. **Verified passing:** zero non-finite floats across all 4,815,200 floats of the packed grass instance buffer, ruling out the silent NaN boot-instancing failure; neither scatter shader link-error string present; the crossed-pair yaw trap decisively clear at 20,000 sampled pairs all at exactly 90.00° with worst deviation 0 and origins shared exactly; quality tiers exact — High cylinder 120,390 / Medium cube 120,390 / Low plane 240,760, **6 batches at every tier**, confirming the crossed pair shares its source's batch; pebbles swapping sphere→cube at Low and the three untiered types untouched throughout; grass measured at 98.9% of all scatter instances, confirming the patch's premise; config shape correct with all three legacy keys absent at runtime; slider positions 4/3/4/3/4 as designed with each landing on its own correct index when driven by the preset; and settings persisting across reload with no repeated reset. Also measured the predicted `rootPartFootprintAabbXZ` over-rejection at Low: exactly ten source blades, **0.008%**. **Not reached** — everything needing a live simulation past frame one: texture animation actually running (items 19-21, the whole point of pass 4), sim-distance cull radius response, distance thinning and decal co-culling, enemy/collectible activation range, and the crossed-pair angle and ground-placement checks.
- [2026-08-23] MAIN: fixed the ARGUS blocker in [Texture.js](engine/v1/handlers/game/Texture.js), and found the gap was wider than the one collection ARGUS hit. Comparing `assignOwnerMeshes` against the texture-registration walk in [NewTexture.js](engine/v1/builder/NewTexture.js) — the authoritative list of collections a texture can be registered from — showed the owner walk also missed `sceneGraph.triggers` and `sceneGraph.scatter`, so patching only `waterVisual` would have left a trigger-owned animated texture to freeze the game identically later. Also established that **pass 4's single-owner premise was false**: it holds only for material textures, whose ids are owner-suffixed, while per-face textures are signature-keyed in `pendingFaceTextures` and deliberately collapse identical faces onto one shared id — the `sea-surface` id proves it, carrying `::face=noise::` and `::anim=…` but no `::animated=` suffix at all. Fix: the walk now mirrors registration, and owners accumulate into an `ownerMeshes` array with a new `isOwnerWithinReach` gating on the nearest consumer. Verified during the fix that `record.mesh` is literally `parts[0]` ([NewObstacle.js](engine/v1/builder/NewObstacle.js)), so obstacle roots are claimed once via `parts` — the registration walk claims both only because its usage map is texture-id keyed and idempotent. `node --check` clean.
- [2026-08-23] ERA: audited the full four-pass performance patch across 12 engine files — **clean, no violations**, flag-only pass, no edits. Substantive reasoning on the three contested points rather than a bare pass: the tier-map discriminators (`value.low === undefined` in both `resolveTier` and `instanceScatterField`) are variant dispatch on a genuine two-member authored union with no fallback branch, not the revalidation-with-substitution that `FORBIDDEN_DEFENSIVE_CHECKS.md` §3.D forbids — and ERA explicitly declined to extend the payload-discriminator principle from `core/validate.js`/`core/normalize.js` boundary handling to engine-owned template JSON without being told to, calling that a guess dressed as a rule reading. Unit instancing clean end to end, including that `localScale` correctly stays raw per `UNIT_INSTANCING.md` §4, and that the crossed pair sharing its `scale` object by reference is safe because `CreateRenderMatrix` and `ScaleVector3` never mutate it in place. Traced `physics/Collision.js`'s imports and `builder/NewEntity.js`'s in turn to rule out a cycle from the two new `handlers/`→`physics/` imports. Confirmed no dangling references to `RenderDistance` or to the four inlined helpers.
- [2026-08-23] DRYAD: audited the same patch — two actionable findings, flag-only, no edits. **(1)** The six-line stochastic cull GLSL was byte-identical in both scatter vertex shaders, and the same file already solves that exact problem with the shared `decalSurfaceProjectionGLSL` constant. **(2)** `NewLevel.js` recomputed the scatter Amount lookup per mesh inside `enqueueScatterBatches` though it is invariant for a whole `BuildLevel` call. Rejected four candidates with reasoning the main agent agreed with: `resolveTier`'s five calls per part are build-time and a data-driven restructure would move the clone-vs-no-clone special casing into a side table rather than remove it; `drawnTextures.add` in `ensureSceneTexture` is the DRYer placement, since relocating it would duplicate per-pass texture enumeration across three passes to save a near-free idempotent `Set` insert; `toRadianVector` is called once but is not exported and reads better as a named callback reference; and the texture-gate costs are already hoisted outside the per-texture loop. Also observed, without recommending action, that grass parts author identical `high` and `medium` entries — authored redundancy following directly from the all-three-keys tier contract having no partial-override mode.
- [2026-08-23] MAIN: ran ERA and DRYAD in parallel over the patch, then applied both DRYAD findings directly rather than routing them to ED, both being small mechanical single-file changes. **Did not take ERA's citations on trust** — independently confirmed that `UNIT_INSTANCING.md` §4 exists and does list `transform.scale` among raw multipliers, that the "templates also exposed via the ENGINE API" exception exists as cited, and that `InstanceEngineTemplates` takes its `structuredClone` snapshots *before* running the instancing routines, so the clone-before-instance ordering the rule requires genuinely holds. Rated DRYAD's GLSL finding higher than DRYAD did and recorded why: the two blocks must stay byte-identical for *correctness*, not merely maintainability, since decals cull in lockstep with their blade — so the duplication was an unenforced invariant, not just a divergence risk. **Surfaced a finding neither agent was scoped to look for**, while verifying the clone ordering: `ENGINE.Blueprints.Scatter` now returns tier maps where it previously returned flat part values, a public contract change introduced by the quality pass. Author ruled to keep it — the contract is "what was authored", and tier maps are now genuinely authored. Recorded as an intentional change, not a deferral. ARGUS still outstanding; nothing browser-verified yet.
- [2026-08-23] ED: implemented texture animation gating — pass 4, the final implementation pass, across [Texture.js](engine/v1/handlers/game/Texture.js) and [Render.js](engine/v1/handlers/Render.js). Two independent gates: distance on the CPU bake, per-frame visibility on the GPU blend. Resolved each animated texture's owning mesh once at entry creation and stored the reference so moving owners track for free, matching both `material.textureID` and `faceTextureGroups[].textureID` since per-face meshes never bind the material id. Wrote a non-exported point-to-AABB squared distance rather than using the mesh centre — `sea-surface` is a large plane whose centre can sit far from a player standing on it, so centre distance would have silently gated off water directly underfoot. Gated only `AdvanceNoiseBake` and the `BeginTextureBake` restart, leaving the phase clock outside. Put the drawn-id record at the single `ensureSceneTexture` choke point, before its cache early-return so both hit and miss paths record, touching none of the five bind sites. Deduplicated the initial and runtime-spawn entry-creation paths into one routine while there. **Independently confirmed void walls need no separate resolution path** — `NewVoid.js` copies its texture ids verbatim from a default mesh already present in terrain or obstacle parts, so the face-group match covers them — and left scatter-batch textures unresolved by design rather than inventing a path. `node --check` clean on both files.
- [2026-08-23] MAIN: scoped pass 4 and verified ED's implementation independently of its report, this pass being entirely engine code with no testGame half. Exploration before planning found that two guards already existed in the blend loop (never-drawn, and non-blend-phase) and should be preserved rather than rebuilt, and that animated textures carry owner-suffixed unique ids — which collapsed the "reverse index rebuilt as things move" the brainstorm had anticipated into a single reference resolved once. Verified the `drawnTextures` add precedes the cache early-return, that the clear sits immediately after the blend call and well before the mesh passes, and that the never-drawn case is fully absorbed by the pre-existing cache guard so a first-frame or newly spawned texture can never be permanently skipped. Checked the clamp math and that it compares against a squared reach. **Recorded a correction to earlier documents:** a far but visible texture does not crossfade indefinitely as the brainstorm and plan claimed — with the bake gated, `readySurface` never arrives, so the existing "settles until the lookahead lands" path takes over and the texture completes its current crossfade then holds. It never freezes mid-motion, which was the property that mattered, and notably the gate reuses that pre-existing mechanism rather than adding one. The brainstorm's decision table was amended to match.
- [2026-08-23] ED: implemented scatter quality tiers — pass 3 of the performance patch, three engine files, testGame excluded as a concurrent-edit surface. Tier-aware instancing in [Instance.js](engine/v1/builder/templates/Instance.js) (`instanceScatterField`, scoped strictly to `instanceScatterTemplates` so the character/object/entity/particle/simulator paths pay no tier check), `resolveTier` plus per-type resolution in [NewScatter.js](engine/v1/builder/NewScatter.js) with cloning after resolution so pre-instanced vectors are never touched twice, the `plane → return 0` short-circuit removed from `getPartHalfHeight`, and tier maps authored on `grass` and `pebbles` only. No signature changed anywhere; `CONFIG` was re-imported rather than threaded as a parameter, per the author's ruling that parameter bloat is the worse trade. **Handled the crossed-pair yaw trap correctly:** pushed the twin inside the same `offsetParts.forEach` iteration as its source, cloning the already-noised rotation and adding 90°, reusing the same `partIndex` so no second noise sample is drawn — expanding in `canonicalParts` instead would have given the twin an independent yaw and silently stopped the planes being perpendicular. Cloned the twin's `position` because `BuildObject` stores `transform.position` by reference, and verified batch sharing holds since the batch key derives entirely from the `part` the twin shares. Independently traced the rotation order through `RotateByEuler` and `buildModelMatrix` to confirm the 90° X stand-up makes the noise yaw spin the blade about world Y. Also removed `canonicalizePartTransform`'s `includePivot` parameter, dead once scatter stopped calling it. Flagged out of scope: `scatterRegistry` is unread surface that now exposes tier maps.
- [2026-08-23] MAIN: scoped pass 3 and split it — ED on the scatter feature, main agent on the unit-instancing correction and testGame. Fixed the `UNIT_INSTANCING.md` violation the author raised: `simDistanceTiers` declared CNU distances as bare numbers, so [Render.js](engine/v1/handlers/Render.js) had to compensate with a `CNUtoWorldUnit` wrapper — the exact "leave raw and convert downstream" case the rule forbids. Instanced at the declaration and updated all three consumers, after confirming there were only three; the `CNUtoWorldUnit` import then left `Render.js` entirely. Corrected a **pass 2 error of its own**: the scatter quality slider had been given the four-level list including `Disabled`, but quality has no off state, so it is now three-level, and `DISTANCE_LEVELS` was renamed `GRADED_LEVELS` since it serves two unrelated settings whose shared property is being always-on rather than anything about distance. Verified ED's half independently of its report — confirmed the `includePivot` removal is behaviour-preserving by construction (scatter was the only caller passing `false`), and traced the Low-tier geometry end to end: `buildModelMatrix` applies scale before rotation, so the blade scales to 0.02 × 0.2 in local space, stands up via the 90° X rotation, and `getPartHalfHeight` resolves to 0.1 × uniformScale — identical to the cylinder tier, so Low blades sit on the ground rather than sinking. `node --check` clean across both halves, both JSON files parse, and a static unresolved-symbol sweep over the edited files came back clean.
- [2026-08-22] MAIN: reviewed the author's manual line-saving refactor of the pass 1 and pass 2 code, at the author's request, checking for breakage ahead of passes 3 and 4. **Found one runtime-breaking defect and fixed it:** [Render.js](engine/v1/handlers/Render.js) called `GetSimDistanceValue()` in `drawScene`'s cull-radius computation without importing it — the `physics/Collision.js` import had been deleted alongside the inlined `GetScatterDrawDistance`, and the replacement call named a symbol never brought into the file. `drawScene` runs every frame, so this was a `ReferenceError` on the first frame of any level render. `node --check` cannot catch it: an unresolved runtime reference is not a syntax error, which is why it survived the author's own checks. Fixed by restoring the single import line rather than reverting the inlining, preserving the author's intent; no cycle is introduced, since `physics/Collision.js` reaches only `core/`, `math/` and `builder/NewEntity.js`. To find it, ran a static unresolved-symbol sweep across all eight author-modified engine files, diffing uppercase-initial call sites against each file's imported bindings and local declarations — `Render.js` was the only genuine hit, the rest being comment text followed by an open parenthesis. Every other inline verified correctly wired. **Nothing else reverted:** the refactor obstructs neither remaining pass, and it improves pass 3 — since `NewLevel.js` now resolves the Amount tier at the call site and threads `scatterMultiplier` through the existing params objects, pass 3 should resolve `Scatter.Quality` the same way and pass it as a sibling parameter, keeping `NewScatter.js` free of `CONFIG` entirely rather than re-importing it as the original plan assumed.
- [2026-08-22] ED: restructured `CONFIG.PERFORMANCE` — pass 2 of the performance patch, six engine files, testGame deliberately excluded as a concurrent-edit surface. `Scatter: { Amount, Quality }`, flat `Particles`/`Animations`, `RenderDistance` deleted after confirming zero readers, `Animations.Active` folded into a `Disabled` tier value, and a new exported `PERFORMANCE_AMOUNT_SCALE` collapsing both multiplier if-chains to single lookups. `GetSimDistanceValue` became a table lookup and gained a sibling `GetScatterDrawDistance` scaling the tier by `0.45`, which `Render.js` now calls in place of pass 1's local constant. Performed the requested reachability check rather than asserting it: `resolveCorrectionFrames` has exactly one call site engine-wide, inside `ResolveEntityAnimation` downstream of the `Disabled` early return, so a zero quality scale is unreachable — and even if reached, `correctionN` of 0 cannot pair with the `correctionCounter > 0` guard the division sits behind, so there is no division-by-zero exposure either way. Marked the `Animations` curve divergence inline so a later dedup pass does not unify it with `PERFORMANCE_AMOUNT_SCALE`. `node --check` clean on all six; greps for `TerrainScatter`, `RenderDistance`, `Animations.Active` and `Animations.Quality` returned zero engine hits. Flagged, correctly, that testGame's then-unedited `applySettings` would now **throw** rather than silently no-op — `cfg.PERFORMANCE.Animations.Quality = …` assigns to a property of a string primitive, and module code is strict — so the two halves had to land in the same session.
- [2026-08-22] MAIN: scoped pass 2 and split it — ED on the six engine files, main agent on testGame, which is outside ED/ERA/DRYAD scope. Ran the two concurrently on disjoint files. Two findings came out of reading the settings plumbing before planning rather than during implementation: renaming a saved key is not neutral, because `loadSettings` backfills absent keys from `DEFAULT_SETTINGS`, so the rename resets stored scatter and animation tiers to `High` instead of preserving them as the no-migration decision had assumed — surfaced to the author, who accepted the reset; and `derivePerformancePreset` was reusing `TIER_LEVELS` as its preset-candidate list, so making that array per-setting would have pinned the preset slider to `Custom` permanently. Gave it a `PRESET_TIERS` list derived from `PRESET_LEVELS` minus `Custom`. testGame edits: per-setting level arrays on `TIER_SLIDER_MAP`, a `TIER_LEVELS_BY_ID` map replacing the `TIER_IDS` set in `ui.js`, `syncSettingsUi`'s four hand-written slider calls collapsed into one map loop matching the `PERCENT_SLIDER_MAP` line above it, and `ui.json` row renames plus a new scatter quality row with `max` raised to `"3"` on the four rows that gained `Disabled`. Confirmed `updateLeveledSliderVisual` needed no change — it already derives fill from `levels.length - 1`. Verified ED's half independently of its report; every `PERFORMANCE` reader across engine and testGame is on the new shape, and searches for all eight superseded identifiers return nothing. Per author instruction ERA, DRYAD and ARGUS were again not run.
- [2026-08-22] ED: implemented scatter distance culling — pass 1 of the multi-pass performance patch, confined to [handlers/Render.js](engine/v1/handlers/Render.js) as scoped. Added `u_cullRadius` plus an identical per-instance stochastic cull block to both `createScatterProgram` and `createScatterDecalProgram`, a non-exported `scatterReachFraction` constant, a per-frame `cullRadius` on the existing `passState`, and the uniform upload at exactly two sites (PASS B after `configureTexturedMeshPass`, and `drawScatterDecalPass` after `beginDecalState`). No fragment shader touched, `configureTexturedMeshPass` untouched, no defensive guards added. `node --check` clean. Verified `physics/Collision.js` imports only `core/`, `math/` and `builder/NewEntity.js` before adding the `handlers/`→`physics/` import, ruling out a cycle. Independently confirmed the main agent's read of the `createLookAtMatrix` CNU/world-unit inconsistency and left it untouched as instructed, adding the observation that the underwater test at the adjacent line carries the same raw-CNU assumption.
- [2026-08-22] MAIN: scoped pass 1 from the settled brainstorm decisions, wrote the implementation plan, and delegated to ED. Resolved the plan's one open question before delegating rather than passing it down as an investigation: instance matrices are world-unit (via `CreateRenderMatrix`'s `toWorldUnit()`) while `GetSimDistanceValue` returns CNU-flavoured numbers, so ED was instructed to convert through `CNUtoWorldUnit` rather than rely on `CNU_SCALE === 1`. Verified ED's work independently of its report — reviewed the full `git diff`, confirmed `beginDecalState` routes through `configureTexturedMeshPass` so `useProgram` runs before the decal uniform upload, confirmed `CNUtoWorldUnit` is exported from [Utilities.js:195](engine/v1/math/Utilities.js#L195), and re-ran `node --check`. Diff matches the approved plan with no drift. Recorded a pre-patch audit baseline in the plan file: the working tree was already dirty (`config.js` debug-flag toggles and alignment reformatting, unrelated to this patch), which matters because the end-of-patch ERA/DRYAD audit is scoped to uncommitted changes and pass 2 will touch `config.js` as well. Per author instruction, ERA, DRYAD and ARGUS were **not** run — audits are deferred to the end of the whole patch, with the author signing off on visuals afterwards.
- [2026-08-22] MAIN/SAGE: drove the terrain-scatter performance discussion item by item to completion; every item in [PERFORMANCE_BRAINSTORM.md](engine/v1/docs/PERFORMANCE_BRAINSTORM.md) now carries an author ruling and the file is ready to scope. No code changed. **Rulings:** scatter gains `Scatter.Amount`/`Scatter.Quality` using tier-keyed *fields* (not tier-keyed part lists) with a raw value meaning constant — the author dissolved the "fields cannot change part count" objection by ruling that a `plane` primitive is deterministically expanded to a crossed pair in code, and that dimensional variance goes through `localScale` rather than `dimensions`; no part-count reduction mechanism, so Quality controls what a blade is and Amount controls how many clumps exist. A `Disabled` tier is added with curve `{0, 0.4, 0.7, 1}` — deliberately gentler than the `{0.25, 0.5, 1}` convention at [Animation.js:64](engine/v1/handlers/game/Animation.js#L64) because Amount and Quality now multiply — with no save migration (a stored `"Low"` keeps its label and gains new meaning) and `Animations.Active` folded into a flat `Animations` tier. Particles get Amount and `Disabled` but **no** Quality: scatter is GPU-bound per instance while particles are real entities in `sceneGraph.entities` and CPU-bound per entity, so a geometry tier would target triangles particles never spent; tiering `physics` mode was considered and rejected as making "Quality" mean behaviour for one system and fidelity for the other. `RenderDistance` is deleted and `SimDistance` becomes the single tier with per-subsystem multipliers (physics/particles 1.0, scatter ~0.45). Scatter culling is vertex-shader collapse with a `smoothstep` tint fade, tiled sub-range draws kept as the escalation path. **Findings that changed decisions:** backface culling is never enabled anywhere in [Render.js](engine/v1/handlers/Render.js), so crossed planes are already double-sided; [buildPlane](engine/v1/builder/NewObject.js#L437-L451) is XZ-oriented and ignores `size.y`, so a blade needs a stand-up X rotation before the yaw duplication; and per-part yaw is `hashNoise(worldX, worldZ, seed + partIndex)` ([NewScatter.js:321](engine/v1/builder/NewScatter.js#L321)) feeding the bounds and opening tests too, so a naively appended duplicate draws a different yaw and the planes stop crossing — the pair must share the source part's seed. Texture animation was found to carry a **second** per-frame cost missed on the first pass: besides `AdvanceNoiseBake`, [Render.js:1300-1335](engine/v1/handlers/Render.js#L1300-L1335) runs a blend pass and periodic `texImage2D` upload per animated texture per frame, and state is keyed by texture ID so no per-object gate is possible; ruled to gate with two signals — distance gates the CPU bake, drawn-last-frame gates the GPU blend — which keeps a far-but-visible texture blending its existing surfaces instead of freezing. Texture animation stays independent of the `Animations` tier. **Rejected:** capless cylinders via an `openEnded` primitive option (a small top cap is invisible *from afar*, which is 4a's job, not a reason to permanently degrade near geometry) and any sub-`low` complexity tier or `primitiveOptions.segments` override (`complexity` is left entirely alone). **Process correction from the author:** current usage counts must never influence engine development decisions — sizing texture-animation gating by "only 2 animated textures exist today" was called out as a cheap write-off rather than analysis, since potential usage is the legitimate input in an unfinished engine. Recorded to memory.
- [2026-08-22] ARGUS: browser-verified `ENGINE.UI.ConvertHTML` in testGame across three phases — all passed, zero console errors for the whole session. **Regression:** startup overlay, all three splash stages, title screen, Settings and the in-game pause menu all rendered and behaved as before across a full title→settings→back→new game→pause→exit→title cycle; no `<style>` tag containing the string `"undefined"` appeared at any point, no new warnings referencing `menu.stylesheet`, and hand-authored screens correctly produced no `engine-ui-style-*` tag at all. ARGUS reached the simulator HUD directly via `ENGINE.Simulator.Start()` — the one patched `CreateUI` site not reachable through menu flow — so all three `stylesheet: null` patches are verified live rather than by inspection. **Conversion:** mixed inline content serialized as `<span>Hello </span><b>world</b><span>!</span>`, so the trailing `!` lands after the bold and the space before `world` survived the whitespace collapse; the stylesheet injected into `document.head` as `engine-ui-style-engine-ui-root`; hovering the Close button applied the full `:hover` styling while that element's payload `events` object was empty, confirming pseudo-classes work with no `pointerover`/`pointerout` actions; a `.sample-late` div appended after render — never in the payload — picked up its stylesheet rules; the `<script>` block was dropped with a `log`-level notice and never executed; form values, `data-*` attributes and kebab→camelCase inline styles all round-tripped. **Lifecycle:** `ClearUI` removed root and style tag together; requesting the same screen twice left exactly one tag with contents replaced rather than appended; switching to a hand-authored screen on the same root removed the tag entirely, so CSS does not leak across screens.
- [2026-08-22] ARGUS: incidental finding outside the tested feature — `ENGINE.Meta.VERSION` returned `0.21.4` while testGame displayed `V0.26.3`, i.e. the `meta.js` constant was stale and testGame was rendering its own hardcoded string rather than the engine value. Resolved by the user in the same pass: `VERSION` removed from [meta.js](engine/v1/core/meta.js) and re-authored as a `Version` literal in the frozen `Meta` namespace in [ini.js](engine/v1/core/ini.js), which is where the version is actually published and therefore harder to leave stale. `ENGINE.Meta.VERSION` → `ENGINE.Meta.Version`; both consumers updated ([Bootup.js](engine/v1/Bootup.js)'s bootup-complete log, and testGame's `ui.js`, which now injects the live version into the `TitleScreen` payload instead of hardcoding it). Verified no dangling `VERSION` import remains engine-wide.
- [2026-08-22] ED: implemented `ENGINE.UI.ConvertHTML` — HTML→menu-payload conversion across the six planned files ([NewUI.js](engine/v1/builder/NewUI.js), [Render.js](engine/v1/handlers/Render.js), [UI.js](engine/v1/handlers/UI.js), [canonSchemas.json](engine/v1/core/canonSchemas.json), [normalize.js](engine/v1/core/normalize.js), [ini.js](engine/v1/core/ini.js)), plus a gap the approved plan missed: `Bootup.js`, `Splash.js` and `Simulator.js` build `menu`-shaped literals and call `CreateUI` directly, bypassing `normalizePayloadSchema`, so `payload.stylesheet` would have been `undefined` rather than `null` and `ApplyRootStylesheet` would have injected `<style>undefined</style>` on every startup overlay, splash step and simulator HUD — fixed at the source with one `stylesheet: null` literal each per `FORBIDDEN_DEFENSIVE_CHECKS.md` §4 rather than by loosening the null check. Converter details worth keeping: text runs are whitespace-collapsed rather than trimmed so `<p>Hello <b>world</b>!</p>` keeps its interior space; orphan text adjacent to element children becomes positioned `{type:"span"}` children, because `BuildElement` sets `textContent` before appending all children and a `text` field therefore cannot round-trip mixed inline content; inline `style` attributes are kebab→camelCase converted because `Object.assign` onto a `CSSStyleDeclaration` silently ignores kebab keys; `droppedTags` covers `link` alongside `script`/`style`, since only top-level `<style>` is hoisted to `<head>` and nested ones would otherwise become element definitions.
- [2026-08-22] ERA: audited the `ConvertHTML` feature — clean, no violations, no fixes needed. Independently verified the two contested calls rather than accepting them: `ApplyRootStylesheet`'s `getElementById` existence checks are **not** a `FORBIDDEN_DEFENSIVE_CHECKS.md` §2 violation (the tag is not tracked by engine state, and the shape matches the already-accepted lazy-create-by-id pattern in [meta.js](engine/v1/core/meta.js) `Cursor` and `UIElement.removeRoot`), and the `stylesheet: null` literal audit is complete — exactly four `CreateUI` call sites exist engine-wide, the three patched plus `ApplyMenuUI`'s, which the schema fallback covers. Casing, module boundaries, comment verbosity and Unit instancing all clean.
- [2026-08-22] DRYAD: reviewed the `ConvertHTML` feature — clean on duplication, complexity and performance; one low-severity finding, flagged only (no implementation authorized). Confirmed the `ConvertHTML`→`ParseHTML` wrapper earns its indirection (every other `ENGINE.UI` entry is imported from `handlers/UI.js`; collapsing it would make this the sole one bypassing `handlers/`), that the build/parse direction mirror should **not** be abstracted together since the two operate on different contracts — a guaranteed normalized definition versus arbitrary DOM producing a sparse raw payload — that no document-wide query sits inside the recursive walk so cost stays linear in node count, and that the shared `g`-flagged `cssUrlPattern` is safe because both call sites use `matchAll`, which clones internally. Finding: `parseElement` computed `wrapText` from `node.children.length`, which counts dropped tags, so a `<script>` sibling of bare text would needlessly wrap that text in a span.
- [2026-08-22] MAIN: orchestrated the `ConvertHTML` pass — planned the feature, delegated implementation to ED, ran ERA and DRYAD in parallel. Applied DRYAD's `wrapText` finding directly as a one-line fix (filter `node.children` by `droppedTags` before the length check) rather than routing a small isolated change back through ED. Reverted a `changelog` entry ED had written — that file is SAGE-only, and the entry also hardcoded line ranges that go stale immediately. Authored [sample.html](engine/v1/testGame/menus/sample.html) as the end-to-end fixture (style block, nested tree, mixed inline content, inline kebab styles, `data-*`, form controls, a `:hover` rule, and a class matching nothing in the payload to prove late-added elements still style) and wired a `"Sample"` screen path into [ui.js](engine/v1/testGame/menus/ui.js). Writing that fixture surfaced the behavioural note now in the changelog: a raw converted payload carries no schema fallbacks, so `payload.rootId` is undefined unless passed in `options`. `node --check` clean on all edited JS and `canonSchemas.json` parses; no browser verification yet — ARGUS not run.
- [2026-08-21] MAIN: scoped and then shipped `PERFORMANCE.Resolution`, a render-scale option in [config.js](engine/v1/core/config.js) surfaced in testGame's settings Performance section — implemented directly, no custom agent (the engine portion is ~5 lines and the bulk is testGame, which is outside ED/ERA/DRYAD scope). Scoping found `syncCanvasSize` ([Render.js:1419](engine/v1/handlers/Render.js#L1419)) sized the backing store 1:1 to `clientWidth`/`clientHeight` (DPR ignored) while the canvas is CSS-sized 100%/100% in a `fixed inset:0` root, so a scaled-down buffer is stretched to fill for free, and that `gl.viewport` plus the perspective aspect both derive from `canvas.width`/`.height` in `drawScene` and follow automatically. Nothing else reads default-framebuffer pixel dimensions — no `readPixels`, no screen-space picking, camera input is pointer-lock deltas, UI is a DOM overlay, and the other `gl.viewport` call is the texture-blend FBO on its own surface. Shipped: the config key as an integer percent; a scale-aware `syncCanvasSize` comparing against the *scaled* target rather than `clientWidth` (the flagged pitfall — the naive check reallocates the drawing buffer every frame at any scale below 100% and negates the win), read per frame so the setting applies live without a level restart; and testGame plumbing across [main.js](engine/v1/testGame/main.js), [ui.js](engine/v1/testGame/menus/ui.js) and a new slider row in [ui.json](engine/v1/testGame/menus/ui.json). Deduplicated while in `main.js`: `updateSensitivitySliderVisual` generalised to [`updatePercentSliderVisual(targetId, value, min)`](engine/v1/testGame/main.js#L98) over an extracted `stepPercent(value, min)`, collapsing clamp/step logic that had been duplicated between the visual updater and the input handler, and `SENSITIVITY_SLIDER_MAP` became `PERCENT_SLIDER_MAP` with a per-entry `min` covering both sensitivity sliders (0) and resolution (5), which `syncSettingsUi` now drives in one pass. Design as built, per the user: percentage slider in 5% steps; independent of the performance preset (out of `TIER_SLIDER_MAP`, so it never forces "Custom"); no floor beyond 5%, which is merely the lowest non-zero step — 0% is excluded only because a zero-size buffer makes the perspective aspect `0/0` = NaN ([Render.js:1885](engine/v1/handlers/Render.js#L1885)), blanking the frame rather than rendering small; supersampling above 100% explicitly out of scope. Saved settings migrate without a wipe because testGame `loadSettings` already backfills missing keys. `node --check` passes on all four edited JS files and ui.json parses; no browser verification yet — ARGUS offered to the user rather than auto-invoked, the change being both visual and behavioural.
- [2026-08-23] ARGUS: second run on the scatter/texture performance patch (passes 1–4), after the owner-resolution blocker fix and the shared-enumeration hardening — **clean, zero defects attributable to the patch and zero console errors** across both stages. 19 of the 22 brief items verified; the three left open are the ones the brief already assigns to the author's visual sign-off (distance-fade appearance, crossed-pair readability at Low, blades sitting on the ground). **Pass 4 executed for the first time**: both in-tree animated textures resolve owners and cycle continuously — `energy-core` (two owner meshes) and `sea-surface` (one) advance `blendRatio` 0→1, transition hold/blend and actually swap surfaces (2 and 4 full cycles respectively in ~5s) at a steady 75–76 fps. The multi-owner accumulation added by the hardening pass proved load-bearing on current content rather than hypothetical: `energy-core`'s two owners measured 22.8 and 62.2 against a reach of 60, so exactly one was in range and any first-owner/last-owner implementation would have been a coin flip. The point-to-AABB choice was confirmed decisively, not incidentally — standing at the corner of the 120×80 `sea-surface` plane the camera sat 65.46 from its centre against reach 60, so a centre-distance implementation *would* have gated the bake off, while point-to-AABB measured 0.83 and the surface kept baking through 3 swaps in 4s. The distance gate was proven to gate in both directions: held genuinely far (711.9 vs reach 21) the bake job stayed null for 6.4 continuous seconds, both textures settled at `blendRatio` 1.0 and *held* rather than freezing mid-motion (the corrected behaviour the brainstorm doc describes), and on return `sea-surface` restarted baking within 405ms with no latch. Two false alarms were chased down and dismissed rather than reported — a non-null `bakeJob` seen 24/24 samples while gated is a *parked* job (`AdvanceNoiseBake` is never called when the gate is closed, so it neither progresses nor costs anything), and an apparent bake-at-distance was the player falling into the death barrier and respawning back into range mid-measurement. Scatter side: 6 batches / 121,713 instances / 6 draw calls at High/High, the pre-patch count unchanged, with batch and draw-call count holding at 6 across *every* density/quality combination, so the crossed pair shares its source's batch; Quality Low rose to 242,083 (+~120,370, matching the grass count, consistent with 1:1 plane duplication) and Density Low + Quality Low gave 96,774 = 0.4002× that, confirming the `Disabled/0.4/0.7/1` curve. Grass rendered at Low, so the silent NaN boot-instancing failure did not occur. `SimDistance` changes the cull radius live with no level restart (verified from an identical camera position), `Disabled` bares the terrain while `Low` is sparse-but-present (the mislabelled-setting defect pass 2 existed to fix), slider geometry matches the design (4 positions on density/particles/animations, 3 on quality/sim distance), the preset derives to `Custom`/`High` correctly in both directions and persists, the one-time density reset to `High` happened as predicted and did **not** repeat, no `RenderDistance` or `Animations.Active` reference survives in engine `.js`, and enemy/NPC activation is unchanged by the sim-distance unit correction (moved in 8/8 samples in range, 1/8 beyond). Stage 2 loaded as regression coverage at 75 fps with legitimately zero animated textures — it has no water and `stone-block`'s blueprint carries `animation.able: false`, so that level payload's `animated: true` is correctly ignored. **One item could not be isolated**: the visibility gate's *hidden* branch, because the renderer object holding `drawnTextures` is module-scoped in [Render.js](engine/v1/handlers/Render.js) and unreachable from the `ENGINE` API. The visible branch is proven, and both catastrophic mis-orderings of the clear point are ruled out by observation (clearing at the wrong point would gate every animated texture off permanently; never clearing would gate nothing); what remains unproven is specifically that a near-but-offscreen texture skips its blend pass. No frame-time measurement was taken — that remains unmeasured across the whole patch.
- [2026-08-23] MAIN: post-verification review of the scatter/texture performance patch — no code changed, one real defect found by source inspection after the author reported aggressive scatter pop-in. **Pass 1's distance fade was never built.** The recorded decision was to fade tint alpha across the boundary so instances dissolve rather than pop; [`scatterCullGLSL`](engine/v1/handlers/Render.js#L374-L383) never assigns `v_tint`, and its `fade` feeds only a threshold against `cullHash`, which derives from `instanceOrigin` and is therefore a fixed per-instance constant — so every blade is binary and only the aggregate thins, with all flips crammed into a ~6.75-unit band at High. Confirmed the fix is possible rather than assuming it: straight-alpha blending is enabled frame-wide ([Render.js:1925-1926](engine/v1/handlers/Render.js#L1925-L1926)), the fragment already computes `texel.a * v_tint.a` and already discards below `0.01`, so the alpha path exists and was simply left unconnected. Recorded the constraint most likely to be lost later — the hash cull must survive any fade work, because collapsing the vertex is the entire performance mechanism and an alpha fade alone would rasterise every blade to the full radius and cost rather than save — and the untested compositing risk that scatter draws with depth write on, so a half-faded blade may punch a hole to the background instead of revealing the blade behind. Three author items are under active discussion (the fade correction, a too-short scatter radius plus a proposed `Ultra` tier, and an `INT_CONFIG`/`EXT_CONFIG` split) — being discussed, not deferred, so deliberately not logged as deferrals; they are tracked in [PERFORMANCE_BRAINSTORM.md](engine/v1/docs/PERFORMANCE_BRAINSTORM.md) until decisions are reached. A fourth author idea — per-entity `render.animatedTexture` / `render.model` flags set from the existing collision-stage `SimDistance` check — was assessed and left unscheduled at the author's instruction; it suits the texture gate because a flag inverts a per-owner point-to-AABB walk into an OR over booleans reusing work already done, but it covers entity-owned textures only (terrain, obstacles and the `sea-surface` water fixture never enter physics), and that hybrid is justified rather than a wart since centre-to-centre distance is correct for small entities and wrong for large planes — exactly why `sea-surface` needed point-to-AABB. [PERFORMANCE_BRAINSTORM.md](engine/v1/docs/PERFORMANCE_BRAINSTORM.md) was rewritten from a design/verification record into a tracking surface for these four open items; it remains temporary. Author's visual sign-off is still outstanding and is best taken after the fade correction lands, since that changes what is being signed off on. Frame time remains unmeasured across the whole patch.
- [2026-08-23] ED: centralised every performance tuning constant into a nested `PERFORMANCE_TIER_SCALE` in [config.js](engine/v1/core/config.js) across seven files — pure refactor, no numeric value changed, `node --check` clean and no stale `simDistanceTiers` reference left. Two judgement calls beyond the plan, both accepted by MAIN: emitted the GLSL-interpolated fade constant as `float(${value})` instead of the specified `.toFixed(2)`, on the grounds that fixed-precision rounding would silently desynchronise shader from config on a future value like `0.875` while `float()` is both type-safe and precision-exact; and deleted two trailing comments at `Render.js`/`Texture.js` that restated what the new key paths now say.
- [2026-08-23] ERA: audited the tuning-constant centralisation — fully clean, no violations, no fixes needed. Verified independently rather than assumed: the `SimDistance` `Unit`s are instantiated once at declaration and never re-instanced or mutated downstream (no `toWorldUnit(true)` anywhere), every tier lookup is a bare access with no fallback or existence guard, key casing follows the convention for properties on exported `FULL_CAPS` objects, `handlers/game/Texture.js`'s new `core/` import is a legal direction, and the shader interpolation resolves at module load rather than per frame.
- [2026-08-23] DRYAD: reviewed the same centralisation — one finding, flagged only (no implementation authorized). Clean on everything else, having confirmed no accidental wiring between the two tier curves, that `GetSimDistanceValue` is called exactly once per frame at each of its three runtime consumers rather than per entity, that the shader template is module-scope and never rebuilt, and that no performance constant was missed (`Resolution`, `Scatter.Quality` and `FrameRate` checked and correctly excluded — none has the tier-multiplier shape). Finding: removing the old "do not unify" comment left the anti-unification guard name-only, and two structurally identical adjacent curves are exactly the shape that invites a future DRY merge; it also correctly observed that `config.js` does not itself follow the no-comments-in-data-objects standard, since `CONFIG` above carries short per-field trailing comments.
- [2026-08-23] MAIN: orchestrated item 3 of the post-verification correction pass — scoped it, delegated to ED, ran ERA and DRYAD in parallel, and resolved DRYAD's finding directly. The resolution produced a reusable ruling on comment placement: a prohibition does not belong on a data object, because comments explain what code *does* and a prohibition is not a statement of function; it belongs at the site that enforces the distinction, phrased as a direct statement of function whose wording implies the rule. Applied as one line in `resolveCorrectionFrames`. Also rejected, during scoping, the `INT_CONFIG`/`EXT_CONFIG` split that item 3 originally proposed — `DEBUG` straddles the boundary and no merge preserves the object identity that live settings depend on. Items 1 (scatter fade correction) and 2 (scatter reach retune plus an `Ultra` tier) remain under active discussion and are deliberately not logged as deferrals.
- [2026-08-23] MAIN: implemented item 2 of the correction pass directly rather than via ED — two values plus a tier in `PERFORMANCE_TIER_SCALE`, and the rest is testGame, which is outside ED/ERA/DRYAD scope. `ScatterCullRadius` 0.45 → 0.70 and a new `Ultra` sim-distance tier at 160 CNU; testGame gained `DISTANCE_LEVELS` so the sim distance slider carries four positions while scatter quality keeps three. The preset was deliberately left spanning `Low`/`Medium`/`High` so `Ultra` resolves to `Custom`. `node --check` clean, `ui.json` parses, and `GRADED_LEVELS` confirmed still bound to scatter quality only.
- [2026-08-23] ARGUS: verified items 2 and 3 together in testGame — ten checks, **zero defects and zero console errors**, including no scatter or scatter-decal shader link error, which was the one place item 3's GLSL `float(...)` interpolation could have failed loudly. Rather than judging screenshots, which could not distinguish a 70-unit radius from a 112-unit one at the available vantage, it instrumented `WebGL2RenderingContext.uniform1f` and captured what actually reaches the GPU: the cull radius uniform measured **24.5 at Low, 70 at High, 112 at Ultra**, matching 35/100/160 CNU × 0.70 exactly, every change applying live within 300ms with no level restart. It also imported [config.js](engine/v1/core/config.js) directly in the page and read the whole object — the three groups exist as designed, `Quantity` and `EntityAnimation` are intact and distinct, `TextureBakeRadius` and `FadeStart` unchanged — and confirmed `CONFIG === window.ENGINE.CONFIG` is still true, so the object identity every live setting depends on survived the refactor. Scatter instancing unchanged at 6 batches / 121713 instances / 6 draw calls; settings persisted with no reset; enemies moved 8/8 in range and 1/8 beyond. Texture animation still resolves two owners for `energy-core` and one for `sea-surface`; held 853 units out against a bake reach of 60, `bakeJob.drawn` froze at exactly 70 across twelve samples, proving the bake is parked rather than merely non-null. DRYAD's unification concern was settled by finding a real discriminator instead of a proxy: the player's `correctionN` is 8/4/4 across High/Medium/Low, where a curve wrongly wired to `Quantity` would have produced 6 at Medium — so the deliberately steeper curve is genuinely in use.
- [2026-08-23] ARGUS: two runtime notes worth keeping, both of which cost time across two runs. A non-null `bakeJob` never means baking is happening — `AdvanceNoiseBake` simply is not called while the distance gate is closed, so a partially complete job sits parked; only tracking `bakeJob.drawn` distinguishes parked from active. And `correctionN` is recomputed only inside the action-changed branch of `ResolveEntityAnimation`, so reading it after changing the `Animations` tier returns a stale value unless an action transition is forced first.
- [2026-08-23] MAIN: reshaped `PERFORMANCE_TIER_SCALE` after the author rejected the nested form outright — rename and restructure only, every numeric value unchanged. The rejected shape grouped values by what they were relative to (`ByTier` / `FractionOfSimDistance` / `FractionOfScatterCullRadius`); the objections were that the names were absurdly long and overexplanatory, and that grouping by base fragmented cohesive subsystems, with scatter's density, cull radius and fade each landing in a different group. This was an over-correction from an earlier complaint that the names were too vague. Three candidates were put to the author — mirror `CONFIG.PERFORMANCE`'s own key path, nest everything derived from `SimDistance` underneath it, or flatten with short compound names — and the flat form was chosen; the mirror form was declined partly because it would have split the shared `Quantity` curve into two independently tunable ones, which is a behaviour decision rather than a refactor. Final shape is three tier curves (`Quantity`, `Correction`, `SimDistance`) plus three scalars (`ScatterOfSim`, `TextureOfSim`, `ScatterFade`). `EntityAnimation` became `Correction`, which names what it actually drives and carries no texture-animation ambiguity. All seven call sites updated and cross-checked against the declared keys with none orphaned; `node --check` clean and no stale `ByTier`, `FractionOf` or `EntityAnimation` reference remains. No browser retest, by the author's instruction that a pure rename does not warrant a test cycle on semantics. The changelog entry describing the old shape was corrected in place rather than appended to, since that shape never shipped.
- [2026-08-23] MAIN: reshaped `PERFORMANCE_TIER_SCALE` a second time, at the author's direction — naming only, no behaviour change. The previous flat form was rejected field by field: `Quantity` and `Correction` both left the reader guessing "of what", `ScatterOfSim` and `TextureOfSim` were judged not even readable, and `ScatterFade` gave no clue what its number represented. `SimDistance` was singled out as the one good field, which set the standard — a plain noun phrase whose value looks like what the name says. The root cause across three failed attempts was that several of these values are dimensionless fractions of something else, which no short flat name can carry; the first attempt answered that by making names maximally explicit and produced `FractionOfScatterCullRadius`, the second by shortening them and produced `ScatterOfSim`. Three genuinely different approaches were put to the author — absolute per-tier distances so nothing is a fraction, fractions under a parent naming the base once, or integer percents with a `Percent` suffix — and the parent-group form was chosen, keeping `SimDistance` as the single source every derived distance follows. Final shape: `Population`, `CorrectionFrames`, `SimDistance`, `ShareOfSimDistance` (`ScatterCull`, `TextureBake`) and `FadeBand`. `FadeBand` 0.15 replaces `FadeStart` 0.85 as a representation change — the shader now reads `u_cullRadius * (1.0 - FadeBand)`, expressing the band's width at the point of use rather than hiding a start point in the constant. All call sites updated and cross-checked against the declared keys with none orphaned; `Collision.js` needed no edit since the `SimDistance` key did not move. `node --check` clean on all seven files, no stale name anywhere.
- [2026-08-23] MAIN: the author took over the performance scaling object and rewrote it; MAIN rewired the six consumers to it. The const is now `PERFORMANCE_SCALING`, keyed to mirror the settings it scales — `SimDistance` holding `Tiers` plus the `Fractions` derived from them, `Density` holding `Scatter` and `Particles` as separate curves, and `Animation.Entities` for the frame-correction budget. Sim distance tiers were retuned to 50/100/150/250 CNU in the same pass, which supersedes the reach figures ARGUS measured at the GPU last run. Two meaning errors in the author's first draft were flagged and fixed by them: the entity animation curve had been filed under `Textures`, which would also have contradicted this patch's decision that texture animation is independent of `PERFORMANCE.Animations`, and the particle count curve had been filed as a fraction of sim distance when it is neither a fraction nor distance-related. All six consumers repointed, every declared leaf path consumed with none orphaned, `node --check` clean on all seven files. The use-site comment in `resolveCorrectionFrames` was removed rather than updated — it existed to stop that curve being merged with the shared quantity curve, and the curves now sit under structurally distinct parents with the declaration carrying its own note. No browser verification of the rewire or the retune yet.
- [2026-08-23] MAIN: three naming attempts at that object were rejected in succession before the author took over, which is worth recording as a pattern rather than an incident. The diagnosis that ended it: these constants were clear in their original scattered form **because the call site supplied the meaning** — a local named `cullRadius` multiplied by sim distance, a curve indexed by the very setting that named it, a curve declared inside `resolveCorrectionFrames`. Hoisting them into a shared object stripped that context, and each attempt tried to cram it back into the key names, producing either vague names (`Quantity`, `Correction`) or grotesque ones (`FractionOfScatterCullRadius`). The lesson for any future centralisation: when values lose their explanatory context by being moved, the fix is to key them so the call site still reads as a sentence — not to make the keys self-contained descriptions.
- [2026-08-24] MAIN: implemented item 1, the scatter distance fade, closing the last open item of the performance correction pass. Shipped in two deliberately separable steps — the dissolve band widened from 0.15 to 0.40 of the cull radius, then a per-instance alpha fade layered on top in the shared `scatterCullGLSL` block — so the second could revert alone if it artifacted. It did not. The two scatter programs needed different handling: the main one folds the fade into `v_tint.a` and needed no fragment change, while the decal program, having no per-instance attribute and a premultiplied fragment, gained a `v_cullAlpha` varying and multiplies the whole `vec4` — scaling alpha alone there would have made decals brighten as they faded. **Author verified visually and signed off**: pop-in is gone, the fade reads as it does in most games, no console errors. The depth-write occlusion artifact was this change's one identified risk and did not appear, because the hybrid keeps only instances near their own threshold partially transparent and the existing `a <= 0.01` discard removes them before they write depth — recorded in the changelog so the earlier blanket rejection of alpha fading is not cited against this technique later.
