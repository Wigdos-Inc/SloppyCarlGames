# Agent Log

Running log of custom-agent activity (ERA, DRYAD, ARGUS, RIGOR, ED, SAGE). SAGE is
responsible for appending an entry here after any agent run that produces a
meaningful finding or change. The weekly status task reads this file and then
empties it (back to this header) after summarizing — so entries only need to
cover "since the last weekly report."

## Entry format

`- [YYYY-MM-DD] AGENT: task — outcome (authorized actions taken, if any)`

Example:

`- [2026-07-09] DRYAD: reviewed NewObject.js tube refactor — found 1 duplicated matrix-multiply, flagged only (no fix authorized)`

## Log

- [2026-07-30] ED: implemented the particle runtime API + runtime-request validate/normalize boundary (phase 3) across `core/canonSchemas.json`, `core/normalize.js`, `core/validate.js`, `core/ini.js`, `core/config.js`, `builder/NewParticles.js`, `builder/NewLevel.js`, `handlers/game/Level.js` and `testGame/main.js` — shipped; all `node --check` passed. Found 3 issues, 2 of which were errors in the approved plan: the plan's `this.offset` field was dead weight and its "follow-the-target stays additive" claim was wrong (Level.js resolves the absolute position before `GenerateParticles`, so `offset` held the absolute point); the plan's "no throw ever reaches the game" claim was false (`structuredClone` throws `DataCloneError` on a callback, and the schema walk ran after it); and `spark`'s `intervalMs` (1200 ms) exceeds its `endTimeMs` (700 ms), so a burst lingered ~500 ms invisible-but-collidable. ED also correctly overrode the plan on the target index's `type` values — the plan named `record.mode`/`mesh.meta.mode`, but those are render/collision modes (`"default"`/`"invisible"`/`"void"`), so a game would have had to write `target: { type: "default" }`; ED used `"obstacle"`/`"terrain"` matching the existing `objectType` vocabulary. ED again wrote to `docs/changelog` despite instruction; the entry was kept and corrected in place rather than duplicated.
- [2026-07-30] Main agent: applied ED's three findings directly — removed the redundant `offset`; reordered `helpers.cloneRequest` to walk-then-clone (the walker drops unknown keys first, so nothing unclonable reaches `structuredClone`, while cloning its output still severs the caller's nested references); and added a `lifespan` field to the base `particleGroup` constructor (`duration.intervalMs`) overridden by `burstParticleGroup` to `duration.endTimeMs`, since `intervalMs` is a recycle cadence and is meaningless as a one-shot's lifetime.
- [2026-07-30] ERA: audited the phase-3 diff (read-only) — CLEAN across FORBIDDEN_DEFENSIVE_CHECKS, UNIT_INSTANCING, CASING and MODULE_GROUPS. Confirmed the `GetActiveLevel`/`IsSimulatorActive`/validator-null/`groups.length` branches are external-API preconditions and canonical sentinels rather than defensive guards; that the unguarded post-validation target read is correct per §5A; that position is instanced exactly once across all three paths (build sweep, runtime reseed, new API); and that `core/validate.js` and `core/normalize.js` import nothing from `handlers/`. Also corrected the main agent's stated rationale for the `cloneRequest` reordering: every field in the `particleRequest` schema decomposes to primitive leaves, so `resolveFieldLayer` already rebuilds fresh objects at each level and the reference leak could not occur for *this* schema — the reordering remains correct on its other two grounds, but the primary justification was overstated.
- [2026-07-30] DRYAD: reviewed the phase-3 diff (read-only) — 6 findings, 2 acted on. (1) `SpawnParticles` hand-rolled a subset of `finalizeSpawn`, skipping `AddTextureAnimationEntries` and the bounding-box refresh — a real parity gap with `SpawnIntoScene`. This also corrected the approved plan, which claimed `finalizeSpawn` was O(N × scene) per burst; in fact only `AddToVisualResources` is per-record and the other two run once per call. (2) `buildSceneTargetIndex` built a full O(scene) index to read exactly one entry. DRYAD also corrected the main agent's framing that the particle class hierarchy was three deep — it is two, with `seedingParticleGroup` and `burstParticleGroup` both extending the base directly — and judged it tight. It reversed its earlier "premature" verdict on tier-multiplier consolidation, judged the sixth copy of the `particle`/`overrides` schema block still acceptable under the file's no-`$ref` convention, and judged the `helpers` sub-objects to be ceremony at one consumer each while noting the author explicitly requested that structure.
- [2026-07-30] Main agent: applied DRYAD's two findings — routed `SpawnParticles` through `finalizeSpawn`, and replaced `buildSceneTargetIndex` with a short-circuiting `findSceneTarget`. The second change initially introduced a bug (reading `request.target.id` before validation would throw on a malformed request), caught and fixed by passing a resolver function instead so the schema check runs before any id is dereferenced. `node --check` passed on all edited `.js` files; `canonSchemas.json` parses (31 roots). No ARGUS run this pass per user instruction — runtime verification of the spark-on-landing path was left to the user.
- [2026-08-02] Main agent: diagnosed and fixed the invisible-spark bug reported against the phase-3 particle API — no custom agents invoked (≈10 lines across 4 files, below the ED/audit bar). Root cause traced through source rather than instrumented: sparks were born inside solid geometry and had their launch velocity deleted on frame one, because `ENTITY_COLLISION` reports the emitter's transform origin (the fall cube's bottom face, on the floor) rather than a contact point, and the pre-existing particle skip in `BroadphaseCollectCandidates` only stopped particles from *being* obstructions, not from colliding *with* the emitter. Fixed by gating entity collection behind a new `includeEntities` broadphase parameter that both call sites set from `entity.type !== "particle"`. Added a `max` (180° half-angle, full sphere) spread tier and switched testGame's spark request to `overrides: { count: 40, spread: "max" }`, which is also the **first exercise of the partial-overrides path** — every prior caller passed `overrides: null`. Confirmed correct: `resolveFieldLayer` ([normalize.js:84-87](engine/v1/core/normalize.js#L84)) fills each absent key with its `null` fallback and does not warn (all override keys are `isExpected: false`), so `resolveRequest`'s `pick` falls through to the template for exactly the omitted keys. Recorded a **rejected design**, not a deferral: per-particle velocity will never be added — parts within a group must stay a rigid formation. **Confirmed working at runtime by the user on 2026-08-02**: sparks are visible, spray in all directions, and the burst does not loop.
- [2026-08-02] Particle system phases 1–3 are now end-to-end confirmed in a real level. The user loaded testGame level 1 stage 2 and verified both halves of the feature: the level-authored persistent generator (the `smoke-torch` obstacle) and the runtime one-shot burst (sparks on the `cnu-fall-cube`'s landing, through `ENGINE.Level.SpawnParticles`). No engine module was verified by ARGUS at any point in this feature — every runtime check was manual, at the user's instruction.
- [2026-08-03] Main agent: planned particle phase 4 (live target tracking + entity generators), with two rounds of user-driven revision. The first draft was rejected for proposing a root-level `particle` field on entities plus an `entity.particle` → `entity.particleGroup` rename to make room for it; the user established the governing rule instead — **an object with a `parts` array accepts `particle` per part only, never at the root; an object with no parts keeps its root field because there is nowhere else for it to go.** That removed most of the planned surface (three root schema blocks, three normalize call sites, the merge plumbing, the `ResolveEntitySource` change and the rename all became unnecessary) and reduced the entity work to one line. The user also directed that the same rule be applied retroactively to multi-part terrain and obstacles, where it was already being violated silently. Two design decisions were settled by the user before planning finished: a per-frame rebuilt target map over a persistent `sceneGraph` index (eleven push/splice sites across five files would each need a hook, and one missed hook produces exactly the desync the feature exists to prevent), and per-part-only entity placement.
- [2026-08-03] ED: implemented particle phase 4 across `builder/NewParticles.js`, `builder/NewEntity.js`, `builder/NewLevel.js`, `handlers/game/Level.js`, `core/normalize.js`, `core/validate.js` and `core/canonSchemas.json` (+105 lines total) — shipped; all `node --check` passed and `canonSchemas.json` parses. Made seven judgment calls beyond the plan, all sound: kept `particleTargetKey` non-exported by inverting the map build to iterate *wanted* keys rather than scene objects; used chained instance math (`.set(...).add(...)`) instead of the plan's `set(AddVector3(...))` pseudocode, per `UNIT_INSTANCING` §7; built `originCorrection` as a `UnitVector3` via `.clone().subtract(...)` rather than a plain `SubtractVector3` result; returned `{ type, transform }` from `findSceneTarget` so the validator can distinguish "object exists, part does not"; and moved burst `offset`/`target` nulling to the normalize boundary so mode owns the field, closing a crash path where a game passing `target` alongside `generator: false` would have run `follow` with a null offset. ED also flagged two behaviour notes the plan had not covered (void-mode carriers, one-frame entity-part pose staleness), both now deferred. ED again wrote to `docs/changelog` despite that being SAGE's file — the entry was verified against source, kept, and corrected in place rather than duplicated; two of its `core/normalize.js` line references were wrong (`809`/`861-862` for what are actually the call sites at `818`/`870`).
- [2026-08-03] ERA: audited the phase-4 diff (read-only) — CLEAN across FORBIDDEN_DEFENSIVE_CHECKS, UNIT_INSTANCING, CASING and MODULE_GROUPS, no fixes needed. Confirmed all four branches the plan pre-declared as look-alike guards are genuinely load-bearing: `follow`'s `targets[key] === undefined` is the despawn-detection mechanism itself and carries the required comment verbatim; `group.targetKey !== null` is a mode test; `SpawnParticles` still re-resolves its target post-validation without re-checking it (§5A); and `validate.js`'s `target.partId ?? null` operates on the genuinely raw pre-schema request, is a first-usage boundary check, and is not repeated downstream. Verified `follow` instances nothing per frame and that `offset`/`originCorrection` are instanced exactly once via the `.clone()` exception. Noted that ED's `originCorrection` implementation is *stronger* §7 compliance than the plan's own pseudocode. No rule gaps exposed, so no rule tightening proposed.
- [2026-08-03] DRYAD: reviewed the phase-4 diff (read-only) — no required fixes. Verified the per-frame contract holds exactly as designed: null fast-path when nothing follows anything, per-collection memoization so each targeted collection is walked once regardless of generator count, and duplicate `targetKey`s (a seeding group and its siblings) collapsing in the `wanted` map before the build starts. Measured `follow` at ~5 short-lived plain objects per group per frame and judged it equivalent to the house-normal `ComposeTransform` pattern with no cheaper scoped path — a scratch-buffer `RotateByEuler` would be a cross-cutting engine change. Confirmed `normalizeRootParticle` is genuine dedup across two call sites and that the ordering hazard was handled (`object.parts` is now populated *before* the root-particle call, the two lines having been swapped). Two low-severity optional items raised and deliberately not acted on: `findSceneTarget` repeats a three-branch `find` instead of reusing the `generatorCollections` map (~6 cosmetic lines), and `SpawnParticles` resolves a generator's target twice per call — once inside validation, once after — which DRYAD explicitly did not recommend fixing, since threading the resolved transform back through the validator's return would couple the two boundaries for a sub-millisecond one-off win.
- [2026-08-03] Main agent: authored phase-4 verification content in testGame (no engine source touched). The `smoke-torch` could not be made to move as-is — it is a single-part obstacle, and `updateEntityMovement` ([Level.js:91](engine/v1/handlers/game/Level.js#L91)) reads `entity.movement` and runs only in the entity pass, so obstacles have no movement path at all. Added an `entity.smoke-torch` blueprint and a `moving-smoke-torch` stage-2 instance: a cylinder part carrying the `smoke` generator at the same `{0, 1.1, 0}` offset as the obstacle version, sweeping world {21,1,3} <-> {21,1,14} at 2 cnu/s with `backAndForth: true`, gravity and collision both false. The static obstacle torch was deliberately kept as the no-regression control, so one level load now exercises both the obstacle-root generator (static — proves origins did not shift) and the entity-part generator (moving — proves following works). First content to exercise entity-part generators at all.
- [2026-08-04] Main agent: planned Simulator particle-generator support, with one user-driven revision. The draft forced the Simulator's `physics: "none"` override through a partial `{ ...overrides, physics: "none" }` spread plus `overrides[key] ?? template[key]` in `resolveRequest`; the user challenged it as a `FORBIDDEN_DEFENSIVE_CHECKS` violation and was right — §1 names `x ?? y` outright and §3B's own bad example is the same shape, while `normalize` guarantees `overrides` is null-or-complete, so the coalescing tolerated a state normalize forbids. Replaced with a module-scoped `overrideKeys` array plus `WithParticleOverride` returning a *complete* six-key set, adding no downstream tolerance anywhere. Two user decisions settled scope: hook the sweep in `Simulator.Load` only rather than inside `SpawnIntoScene` (which would also have closed the runtime-spawned-entities deferral but changed game-side behaviour), and route the Simulator's physics problem through a request-level override rather than running the physics pipeline for particle entities in the simulator branch.
- [2026-08-04] ED: implemented Simulator particle generators across `builder/NewParticles.js`, `builder/NewLevel.js`, `handlers/game/Level.js` and `handlers/game/Simulator.js` — shipped; all `node --check` passed. Followed the plan without deviation except for one check the plan left open: `RotateByEuler` in `NewLevel.js` did become genuinely unused once the request mapping moved out, and was removed from the import. Placed the `Load` sweep after `initSimulatorTarget` rather than before it so the "Simulator loaded" log stays last. Did **not** write to `docs/changelog` this pass, correcting the recurrence noted in the two prior ED entries.
- [2026-08-04] ERA: audited the Simulator particle diff (read-only) — CLEAN, zero violations. Independently confirmed the `WithParticleOverride` redesign is a correct fix for the rejected `??` draft, on the grounds that `null` is a schema-documented sentinel (`fallback: null` on the container and all six keys) that `core/normalize.js` itself tests the same way at [:772](engine/v1/core/normalize.js#L772) — branching on an authored sentinel, not fallback substitution. Verified the rewritten `resolveRequest` is behaviourally equivalent and that no `??`, optional chaining or `undefined` test appears anywhere in the new code. Cleared the `Simulator.js` mode branches as dispatch on a field validated by `allowedValues`, the `particle !== null` filters as selection over canonical data, and the `null` viewer position as `IsBeyondSimDistance`'s documented opt-out contract rather than an invented fallback.
- [2026-08-04] DRYAD: reviewed the Simulator particle diff (read-only) — one medium finding. `SpawnParticles` and `SpawnParticleRequests` duplicated the five-argument `GenerateParticles` call plus push plus `finalizeSpawn`. DRYAD correctly excluded `updateParticles`' sibling-reseed loop from its own proposed fix, since siblings deliberately skip `finalizeSpawn`. It also assessed and cleared the `ParticleGeneratorRequests` extraction as genuine deduplication rather than code-shuffling, the `overrideKeys`/`WithParticleOverride` design as already minimal under the no-`??` constraint, the `ParticleGeneratorRequests` vs `targetTransform`/`findSceneTarget` overlap as two genuinely different jobs (enumerate all parts vs resolve one named-or-default part), and both `clearParticles` and the new simulator-branch `updateParticles` call as negligible cost.
- [2026-08-04] Main agent: applied a variant of DRYAD's finding rather than its literal suggestion. DRYAD's proposed helper did generate → push → `finalizeSpawn` and returned the groups, which would have moved `SpawnParticles`' zero-group early return to *after* push and finalize — wasted work on every sim-distance-culled burst. Instead extracted `generateForScene(request, viewerPosition, sceneGraph)`, which deduplicates only the five-argument resolution but does so across **three** call sites including the sibling-reseed loop DRYAD had ruled out, leaving each caller's ordering untouched. `node --check` passed.
- [2026-08-04] Main agent: diagnosed and fixed a Simulator centring bug the author reported while verifying the particle pass — no custom agents invoked (~10 lines, one file). **Not caused by the particle pass**, and traced rather than assumed: `Simulator.js` was last committed at `e034d67` (the obstacle/terrain expansion), and nothing in the particle work touches placement. Two distinct routes to the same symptom: terrain/obstacle use their absolute authored `position` verbatim against a disc that sits at `{10, 0, 10}`, while entities — whose blueprint `rootTransform.position` is already `{0,0,0}` — are displaced instead by `BuildEntity` teleporting the root onto the movement path whenever `speed > 0`, since surface-local `start`/`end` resolve against the disc origin. The entity half only became reachable when the previous pass gave a Simulator-loadable entity a movement path. **One discarded fix worth recording:** neutralizing placement in `mergeSimulatorEntity` (zeroing `rootTransform.position` and `movement.start`/`end` post-merge) was written, then reverted on inspection — `mergeTemplateRefEntity` returns an object with no `movement` key and a possibly-null `rootTransform.position`, so that edit would have thrown on any template-ref entity and could only have been salvaged with a shape guard. Re-seating the *built* object in `Simulator.Load` instead works on a uniform shape and needs no guard.
- [2026-08-03] Main agent: fixed the recurring docs-report complaint at its root, after the author reported it for at least the second time. `.claude/commands/sage.md`'s Output Format mandated a line count table on every write and used a `.md` file as its worked example, contradicting CLAUDE.md's "never count documentation" rule — and because SAGE loads inline, its format won and its report displaced the engine summary entirely. sage.md now forbids line count tables in every mode and caps the `log` report at one line per file; CLAUDE.md gained two rules — a SAGE call is never the last thing in a pass, and documentation content is never reported to the user.
- [2026-08-03] Main agent: independently verified ED's report rather than accepting it — re-ran `node --check` on all seven changed files, confirmed the line-count deltas against `git show HEAD:`, confirmed `canonSchemas.json` parses, and spot-checked every line reference in ED's changelog entry (finding the two wrong `normalize.js` anchors). Also chased and closed one latent crash path the audits did not cover: `NewEntity.js`'s new `particle: source.particle` reaches `CloneTemplatePart` → `cloneParticle`, which throws on `undefined` rather than returning null, and `particles.json`'s prototype part has no `particle` key — the path is safe only because [`instanceModelPart`](engine/v1/builder/templates/Instance.js#L41) defaults it to `null` at boot for particle templates too.

