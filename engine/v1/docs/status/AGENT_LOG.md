# Agent Log

Running log of custom-agent activity (ERA, DRYAD, ARGUS, RIGOR, ED, SAGE). SAGE is
responsible for appending an entry here after any agent run that produces a
meaningful finding or change. The weekly status task reads this file and then
empties it (back to this header) after summarizing — so entries only need to
cover "since the last weekly report."

## Entry format

`- [YYYY-MM-DD] AGENT: task — outcome (authorized actions taken, if any)`

Example:

`- [2026-07-09] DRYAD: reviewed NewObject.js tube refactor — found 1 duplicated matrix-multiply, flagged only (no fix authorized)`

## Log

- [2026-07-20] ED: implemented the player-on-entity-pipeline refactor (rewrote `player/Model.js` as assembler, slimmed `player/Master.js` to orchestrator, deleted the bespoke `collision.profile` system, added `carl`) — returned clean; caught and prevented a `simRadiusPadding` 24→8 regression that `BuildEntity` would have introduced, and removed a redundant `rebuildBounds` passthrough wrapper. Then applied the approved post-audit fix pass.
- [2026-07-20] ERA: audited the refactor — found `loadDecalBitmaps` mutating the shared `characterData` template in place (high; a transient decal-load failure would drop a decal for the session), a `buildPart` `dimensions` non-clone (medium latent aliasing), and 3 casing misses (`ComputeCapsuleFromAabb`, `playerSurfaceId`, `CharacterData`); confirmed layering, no-defensive-checks, and single-instancing all held. Flag-only (no edits); all flagged issues addressed in the follow-up fix pass.
- [2026-07-20] DRYAD: reviewed the refactor — confirmed the dedup landed with no leftover duplicated math or dead code; flagged the AABB capsule recomputed per-frame by two physics consumers and the full 43-part model refresh at 6 physics sync points (recommended profiling, not a speculative change). Flag-only, no edits.
- [2026-07-20] ARGUS: runtime-tested the refactor in testGame — found 2 blocking bugs preventing player init for both characters (single-pass part index requiring parent-first order; carl generated-texture missing `secondary`); neither a physics regression (the `collision.profile` removal could not be exercised). Both fixed directly by the main agent afterward; runtime re-verification left to the user.
- [2026-07-21] Main agent (no ERA/DRYAD/ARGUS/ED — app-side work outside engine scope): implemented the Simulator player/entity mode split in `carlGames/simulator/` (new `playerMode.js`, reworked `main.js`/`main.css`), resolving the 2026-07-20 deferral. 3 Explore research subagents mapped the Simulator wrapper + `ENGINE.Simulator` validation path, the canonical player schema (`characters.json`/`player/Model.js`/`builder/NewEntity.js`), and the entity↔player divergence (part-level `color` → `generated.secondary` alias). Verified via `node --check` + a 23/23 Node unit test of `playerMode.js` against the real `characters.json` (all 3 canonical characters valid, all drift-class negatives rejected, synth deep-clone non-mutating, export format correct); browser render verification left to the user.
- [2026-07-21] Main agent (no custom agents): fixed Simulator Player-mode decal paths — player `imagePath`s are authored relative to `engine/v1/player/` and only resolve through the player pipeline ([player/Model.js:129](engine/v1/player/Model.js#L129) anchors via `new URL(imagePath, import.meta.url)`), but the Simulator previews players through the entity pipeline, whose [core/normalize.js:679](engine/v1/core/normalize.js#L679) fetches `imagePath` with no base URL — so every player decal 404'd against the Simulator document. Added a `resolveDecalPaths` rewrite in `carlGames/simulator/playerMode.js`'s `synthesizePlayerEntity` render clone (top-level + nested `sources`, absolute-URL passthrough), leaving the registry/export copy's canonical `../assets/...` paths intact so "Copy Player JSON" still round-trips. Verified via `node --check` + Node URL-resolution checks and on-disk asset confirmation; browser decal render left to the user.
- [2026-07-21] Main agent (no custom agents): reworked the Simulator player/entity split to be tag-driven after user feedback that it was too narrow (Player mode only listed already-correct canonical characters, so validation never exercised WIP drafts). Established conclusively that the engine's Simulator/Cache path has no "player" objectType — [meta.js:327](engine/v1/core/meta.js#L327) `ENTITY_TYPES` and [canonSchemas.json:1737](engine/v1/core/canonSchemas.json#L1737) `objectType` allowedValues both exclude it, so [validate.js:307](engine/v1/core/validate.js#L307) routes a `player`-typed def to the `levelObject` schema (fails, silently skipped) and the entity-building sites ([Simulator.js:207](engine/v1/handlers/game/Simulator.js#L207), [normalize.js:1068](engine/v1/core/normalize.js#L1068), [NewTexture.js:326](engine/v1/builder/NewTexture.js#L326)) all gate on `ENTITY_TYPES`; players build via a separate path (`player/Model.js` + `characters.json` + the `levelPlayer` spawn schema). Resolution kept entirely app-side (no engine work permitted): `type: "player"` is now a Simulator-only tag the engine never sees — unified `localStorage["entities"]` store partitioned by tag (with one-time migration of legacy `localStorage["characters"]`), player defs synth-cache on load as `objectType: "entity"`, `validatePlayerEntry` accepts the tag / rejects other `type` values / flags entity-convention decal paths. `carlGames/simulator/main.js` (+19) and `playerMode.js` (+25). Verified via `node --check` + a 14/14 Node unit test; browser verification left to the user.
- [2026-07-22] ED: implemented boot-time engine template instancing — created [builder/templates/Instance.js](engine/v1/builder/templates/Instance.js) (`InstanceEngineTemplates()` clones the four template singletons into `raw` before instancing, then three private sisters instance in place, returns `{ raw, instanced }`), rewired [ini.js](engine/v1/core/ini.js) to drop its four template-JSON imports and feed `raw` into `Blueprints`, and removed the import-time IIFEs + local `toUnitVector3` copies from [Model.js](engine/v1/player/Model.js) and [NewScatter.js](engine/v1/builder/NewScatter.js). No idempotency guard (called once by ini.js). `node --check` passed; returned clean.
- [2026-07-22] ERA: audited the boot-time instancing refactor — CLEAN, no violations across FORBIDDEN_DEFENSIVE_CHECKS / UNIT_INSTANCING / CASING / MODULE_GROUPS (confirmed no idempotency or type guard, clone-before-mutate ordering, correct export/local casing, and that `builder/templates/Instance.js` importing `player/characters.json` is a legal same-tier import, not an abuse of the `normalize.js` exception). Applied the authorized rule-doc amendment: added an "API-exposed templates" exception subsection to [UNIT_INSTANCING.md](engine/v1/docs/rules/UNIT_INSTANCING.md) §1 (boot-time canonicalization after the raw API clone; non-exposed engine JSON still uses the import-time IIFE).
- [2026-07-22] DRYAD: reviewed the boot-time instancing refactor — flagged one genuine duplication (the identical 4-line per-part transform block repeated across `instanceCharacterTemplates`/`instanceBlueprintTemplates`, 3-line subset in `instanceScatterTemplates`), trimmed an over-long comment (applied, −3 lines), confirmed the `toUnitVector3` split from `normalize.js` intentional and the structuredClone-then-instance flow non-wasteful. The flagged duplication was then extracted into a module-scoped `canonicalizePartTransform(part, includePivot)` helper by the main agent (`node --check` passed).
- [2026-07-22] ED: implemented whole-model floor grounding in [builder/NewEntity.js](engine/v1/builder/NewEntity.js) `buildModel` — added a post-`applyModelPose` finalization that translates the model so its `computeEntityAabb` floor meets the plane (`groundPlaneY - aabb.min.y`), then re-poses; kept per-root grounding as the assembly baseline. `node --check` passed; returned clean. NOTE: also added verbose multi-line comment blocks (4-line Factor-4 + 4-line finalization) in a file whose surrounding comments are all single-line — comment bloat, trimmed afterward (see main-agent entry).
- [2026-07-22] ERA: audited the floor-grounding change — CLEAN across FORBIDDEN_DEFENSIVE_CHECKS (correctly omits a degenerate all-`addsToBounds:false` guard per fail-fast), UNIT_INSTANCING (in-place `.y` mutation of existing `UnitVector3`, no re-instancing; matches existing precedent at NewEntity.js:261), CASING (`groundPlaneY`/`groundingAabb` lowerCamelCase), and CNU_VS_WORLDUNITS (all CNU-space). Flag-only, no edits.
- [2026-07-22] DRYAD: reviewed the floor-grounding change and the deferred double-`computeEntityAabb` question — confirmed the two AABB scans are minimal-necessary (moving entities are repositioned by the movement lerp after `buildModel` returns, so a post-reposition scan is unavoidable, and the pre-correction scan is a hard prerequisite for the grounding offset); recommended leaving the two-scan structure as-is, no signature change. Flagged the new finalization comment block as exceeding the concise-comment style but only at "low severity." Flag-only, no edits.
- [2026-07-22] Main agent: shipped the floor-grounding fix (ED engine change + ERA/DRYAD audit, both effectively clean) and made the app-side `ent_carl` `rootTransform.position.y` `0.18 → 0` edit. FAILURE recorded honestly: ED added verbose multi-line comments where every neighbor is single-line (comment bloat); DRYAD under-flagged it as merely low-severity; the main agent's first trim was partial (2 lines, still not matching the single-line neighbors). User called all three out as an ongoing pattern — verbose comments becoming the new code-bloat vector now that defensive-check bloat is resolved via FORBIDDEN_DEFENSIVE_CHECKS. Resolved: both comment blocks cut to single lines matching surrounding style (`node --check` passed). Standing correction: comment verbosity is now to be treated as a first-class ERA/DRYAD failure and screened by the main agent before agent handoff. ARGUS testGame regression check still pending (awaiting user go-ahead).
- [2026-07-22] Main agent (no custom agents): fixed a Simulator App runtime crash, `Uncaught ReferenceError: loadCanonicalCharacters is not defined` at `main.js:63`. Root cause: stale code left over from before the 2026-07-22 boot-time template instancing refactor — `carlGames/simulator/main.js` still awaited an async `loadCanonicalCharacters()` (never defined) to rebuild `playerRegistry` once canonical characters "arrived," a holdover from when the Simulator needed its own fetch of `characters.json` before `ENGINE.Blueprints.PlayerCharacters` existed. Since `Instance.js`/`ini.js` now populate `ENGINE.Blueprints.PlayerCharacters` synchronously inside `Initialize()` during `StartEngine()`, the canonical data is already present when `rebuildRegistries()` first runs at [main.js:57](carlGames/simulator/main.js#L57). Deleted the dead async block; confirmed no other reference to the removed `canonicalReady` binding or its rebuild/refreshDropdown calls. `node --check` passed; browser verification left to the user.
- [2026-07-22] Main agent (no custom agents): diagnosed the Simulator App bug where player-mode Carl clips into the ground while entity-mode Carl sits correctly. Root cause: [NewEntity.js:243-262](engine/v1/builder/NewEntity.js#L243-L262) `buildModel` grounds only the root part (`parentId === "root"`, Carl's `body` sphere) by half its own height, never the whole model's true lowest point; legs/toes attached below `body` hang past that. `entities.json`'s `ent_carl` masks the gap with a hand-authored `rootTransform.position.y: 0.18`; canonical player `carl` in `characters.json` has no `rootTransform` field (real spawns get grounding from per-level `spawnPosition` authoring instead), and the Simulator's `playerMode.js:281-284` `synthesizePlayerEntity` hardcodes the synthetic `rootTransform.position` to `{0,0,0}` with no spawnPosition context to compensate from. Confirmed a longstanding root-part-only grounding limitation, not a regression. No code changed (user asked for investigation only); see DEFERRED.md for the pending fix-approach decision.
- [2026-07-21] Main agent (no custom agents): diagnosed the Simulator App bug where engine player characters render with rotations lost (Carl's tentacles stand vertical, legs sink into the floor) while rotation-free characters look fine and native engine/testGame rendering is correct. Root cause: `ENGINE.Blueprints.PlayerCharacters` is exposed **post-instancing**, and the Simulator re-normalizes it through the entity pipeline, double-converting rotations. [Model.js:18-47](engine/v1/player/Model.js#L18-L47)'s IIFE instances the shared `characters.json` singleton in place at import (degrees→radians, positions/dimensions/pivot→`UnitVector3`, thickness→`Unit`); because [ini.js:19](engine/v1/core/ini.js#L19) pulls Model.js in transitively (via [Master.js:10](engine/v1/player/Master.js#L10)) that IIFE runs during the import phase, before `Initialize()` reaches its [ini.js:46](engine/v1/core/ini.js#L46) `structuredClone` — so the API blueprint holds already-radian, prototype-stripped `{x,y,z,type}`/`{value,type}` bags, and [normalize.js:734](engine/v1/core/normalize.js#L734) re-applies `toUnitVector3(…,"degrees").toRadians(true)` a second time, crushing every rotation ~57× to ≈0 (tube `thickness` also breaks via `normalizeNumber` fallback). Only Carl exposes it (0°=0rad for every other character); side effect: the canonical-character Edit view also shows instanced garbage. Confirmed [Master.js:80](engine/v1/player/Master.js#L80) is the sole consumer of instanced `CharacterData`, at post-boot spawn. Recommended Direction B — defer the IIFE to an idempotent `InstanceCharacterTemplates()` called at the end of `Initialize()` after the clone, keeping the API raw and `UNIT_INSTANCING` intact; rejected the "early structuredClone" variant as import-order-fragile. No code changed (an exploratory app-side edit was made then fully reverted per user, who reaffirmed the Simulator must consume via the ENGINE API, not a direct import/fetch). Engine implementation deferred to ED pending user go-ahead and variant choice.
