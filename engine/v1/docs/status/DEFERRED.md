# Deferred Work

Explicitly deferred implementations or decisions — work that was started, considered,
or raised during a task and consciously postponed, along with who deferred it and why.

This is NOT the idea backlog (see `engine/v1/docs/todo` for that). Entries here should
only be things that came up mid-task and got pushed off, not general feature ideas.

Entries persist across weekly reports until resolved, then they are **deleted** —
not checked off, not annotated as done. SAGE sweeps this list on every `log` call
and removes what the pass resolved; any other agent may delete a resolved entry too.

## Entry format

`- [ ] [YYYY-MM-DD] Item — deferred by WHO, because REASON`

Example:

`- [ ] [2026-07-09] Camera.js CNU_SCALE calibration — deferred by ED, because it depends on render pipeline stabilizing first (see CNU_VS_WORLDUNITS.md Section 4)`

## Pending

- [ ] [2026-09-14] A 90° surface cannot degrade to `sliding`, so one dropped frame latches grounding off permanently — deferred by the author, because it is its own scoped pass and he wanted the orientation and input defects judged first. From a world-up reference the delta to a vertical normal is 90°, which exceeds both `Sliding` limits ([config.js](engine/v1/core/config.js#L124) — 85 water, 75 air), so [`ClassifySurface`](engine/v1/physics/Correction.js#L58) returns `"wall"`, `pushHit` discards the hit, `resetSurfaceState` runs, and `referenceNormal` stays world up — self-latching, because every later frame classifies the same way. Measured: 117 consecutive `"none"` frames while the probe sat at exactly the rest distance, and the player coming to rest stationary in `"sliding"` on a 61.4° face he had just walked up, recovering only after backing down to 17.6°. Unlike every other surface, a vertical one cannot even fall back to `sliding`. This now couples to the carried input frame: re-seeding on a grounding change means control stability is downstream of grounding stability, so this pass will read worse than it is until the latch is closed.
- [ ] [2026-09-14] `support = up.y` disables braking entirely on a vertical wall — deferred by the author, because it only becomes reachable now that climbing works, and he chose to judge it in playtest first. `support` scales both deceleration terms in [`UpdateMovement`](engine/v1/player/Movement.js) — the reverse-intent burst and the no-input decay — so at `up.y = 0` both are multiplied by zero and lateral velocity bleeds off through drag alone, leaving no way to arrest a drift while wall-running. Changing it is not local: the `decel·cosθ` against `g·sinθ` relationship is tuned and moving it affects feel on every slope in the game, not just walls.
- [ ] [2026-09-11] The capsule debug wireframe draws incorrectly — deferred by the author, because it is a debug-visualisation fault with no gameplay consequence and the slope physics work takes priority. With bounds display enabled the player's capsule does not render as a clean capsule, does not appear rotated, and its cylindrical section is bent sideways. The underlying bounds were measured directly and are correct: the placed capsule's segment tilt matches the player's pitch exactly at 0°, 30° and 60°, with local `segmentStart` moving from y 0.380 / z 0.010 at rest to y 0.181 / z 0.334 at 60°. The collider genuinely rotates, so the fault is in [`drawDetailedBounds`](engine/v1/handlers/Render.js#L1075) rather than in the collision data. Worth knowing for whoever picks it up: the capsule is radius 0.51 with a segment only 0.11 long — roughly 91% sphere — so rotation is nearly invisible on it regardless, and the visible effect of a tilt is the whole collider displacing forward and down rather than appearing to turn.
- [ ] [2026-09-05] Void classification thresholds are absolute CNU, so the same content classifies differently at a different world scale — deferred by MAIN, because the lvl2 rescale only needed the result verified, and changing a correctness-critical predicate is its own scoped pass. **The finding stands on the source, but its numbers below were measured under a uniform 1/4 rescale that the author rejected and that has since been reverted — treat them as a demonstration of the effect, not as current level state.** [`minimumOpenFaceArea = 0.000001`](engine/v1/builder/NewVoid.js#L16) and [`interiorSampleDepth = 0.001 cnu`](engine/v1/builder/NewVoid.js#L15) do not move with the geometry. Rescaling lvl2 by 1/4 shrank every piece area 16× and reclassified each openFace piece whose area sat between 1e-6 and 1.6e-5 from inside to outside: openFaces 10634 → 7024, maxPerHost 5513 → 3090, carved output 14849 → 15265 triangles as those pieces are retained as geometry instead. The change is confined to `hub-platform` and `underwater-massif` — the two hosts carved by the tube and tilted-slab voids already named below as the sliver generators — while the four hosts carved by ordinary box and cylinder voids are identical triangle-for-triangle, and the relation set, host list and void-wall count are unchanged. No collision consequence today, since carved geometry is render-only. What is unresolved is whether these should be relative to host or void extent so that classification is scale-invariant; a fix must not silently drop a piece the current ray would keep, and wants before/after triangle counts at two scales as its gate.
- [ ] [2026-09-07] The physics hot path logs at all — deferred by MAIN, because it was raised alongside session log persistence (since removed) but is a separate question the author has not ruled on. [`ApplySurfaceCorrection`](engine/v1/physics/Correction.js#L113), [`ApplyGroundSnap`](engine/v1/physics/Correction.js#L140) and [`runPhysicsLoop`](engine/v1/physics/Master.js#L155) each `Log` per tick while the player is grounded and moving. `isDuplicateOfLatest` suppresses the repeats, so only ~2 entries are added per 60 walking ticks and this was never the cause of the walking framedrops; what remains is whether per-tick instrumentation belongs in the physics loop at all. Note the in-memory `logs.all` still grows without bound for the session — now a memory cost only, since the per-entry serialise is gone. Distinct from the slowdown the author reports with the dev console left open, which is Chrome's own `console.log` instrumentation.
- [ ] [2026-09-04] Coplanar duplicate void wall geometry where two voids share a boundary plane — deferred by MAIN, because both surfaces are legitimately on the union boundary and the neighbour-subtraction fix cannot remove either. `hub-pierce-void` and `hub-pierce-mouth` both line x=618 and x=662 across their overlapping z range in lvl2: the material behind each is real host geometry, so neither is dropped, and the two coincide. The result is z-fighting rather than a blocked path, and the author has not reported it. Closing it needs coplanar merging across a host's linings — a different operation from the per-piece subtraction in [`buildHostLining`](engine/v1/builder/NewVoid.js#L311).
- [ ] [2026-09-03] `PointInsideMesh` is the hot spot in both void build paths — deferred by DRYAD, because restricting its candidate set safely is an algorithmic change to a build-time correctness-critical function, not a mechanical hoist. It is a linear parity ray over *every* triangle of the void solid ([math/Collision.js](engine/v1/math/Collision.js)), called per candidate piece, so a ~2000-facet tube pays that scan repeatedly. A spatial structure built once per `voidSolid` and shared by `pieceInsideVoid` and `resolveCavitySign` would fix it, but any prefilter must not silently exclude a triangle the semi-infinite ray would otherwise cross. Wants its own scoped pass with before/after triangle-count verification against the lvl2 numbers.
- [ ] [2026-09-03] The void classification path is now the dominant build cost — deferred by DRYAD, because any speedup must preserve an ordering the numeric gate depends on. `clipHostSurfaceToVoid` costs roughly 2.5s of the lvl2 build against 272ms for the carve. Spatial partitioning of void triangles is the obvious fix, but the dedup key insertion order in [`collectCrossingPlanes`](engine/v1/builder/NewVoid.js#L184) determines the openFace decomposition, and scatter's footprint rejection reads per-openFace-triangle AABBs — so the output must stay identical triangle-for-triangle, not merely equivalent in area.
- [ ] [2026-09-02] `nullable: false` is honoured only wholesale — deferred by MAIN, because per-void granularity has never been the flag's meaning. Such a host is filtered out of the carve and void-wall host lists entirely ([NewVoid.js:414](engine/v1/builder/NewVoid.js#L414), [:444](engine/v1/builder/NewVoid.js#L444)), so it is never carved by *any* void, including ones it legitimately intersects elsewhere in the level. **Updated 2026-09-03:** the original justification — that per-host would need one stencil bit per host and does not fit in 8 bits — is obsolete now that carving is geometric; per-host granularity would be cheap. What remains is a semantics question about what the flag should mean, not a technical blocker.
- [ ] [2026-09-02] Per-draw backface culling for closed opaque meshes — deferred by MAIN, because it cannot be switched on globally and doing it properly is its own pass. Worth real frame time (roughly half the rasterized fragments on opaque geometry), but [`buildPlane`](engine/v1/builder/NewObject.js) is a single quad, and water surfaces, decal quads and debug wireframes are all one-sided — they would vanish from one side. Void wall meshes are a further case: they are wound outward relative to the void *solid*, so they are the one surface meant to be viewed from inside and would need reversed winding or an exemption. [`CONFIG.DEBUG.LEVELS.BackfaceCulling`](engine/v1/core/config.js#L20) exists as the diagnostic.

- [ ] [2026-08-23] Scatter-batch textures have no owner resolution in the texture-animation distance gate — deferred by ED and main agent, because a scatter texture's consumer is an entire object's scatter field rather than a single mesh, so it needs a different resolution shape, and no scatter template authors `animated: true` today. Building for it would have been guesswork at a shape that does not exist. [`assignOwnerMeshes`](engine/v1/handlers/game/Texture.js) walks the shared [`ForEachTexturedMesh`](engine/v1/builder/NewTexture.js) enumeration, which yields meshes only; a scatter *batch* registers by batch id rather than through a mesh, so such a texture resolves to an empty `ownerMeshes` array — and an empty array means the texture silently never bakes rather than raising. The shared enumeration removes the drift route into that state but not the state itself. Void walls need no separate handling — [`NewVoid.js`](engine/v1/builder/NewVoid.js) copies `material.textureID` and `faceTextureGroups` verbatim from a default mesh that is itself in `sceneGraph.terrain` or an obstacle's parts, so the existing face-group match already covers them.
- [ ] [2026-08-23] `sceneGraph.visualResources.scatterRegistry` is dead surface that now exposes quality tier maps — deferred by ED, because deleting an unused public-ish field is a cleanup unrelated to the scatter quality feature and widening the pass mid-patch was the wrong trade. [`NewTexture.js`](engine/v1/builder/NewTexture.js) assigns it `VISUAL_TEMPLATES.scatterTypes`, and nothing in `engine/v1/` including `testGame/` ever reads it. Harmless while unread, but as of the quality pass those scatter parts carry `low`/`medium`/`high` tier maps on `primitive`, `localRotation` and `localScale` rather than flat values, so any future consumer would silently receive a shape it does not expect. Either delete the field or resolve tiers before publishing it.
- [ ] [2026-08-22] Camera-side CNU/world-unit inconsistency in `handlers/Render.js` — deferred by main agent, because it is pre-existing rather than introduced by the scatter-culling pass, and widening a deliberately narrow pass mid-patch was the wrong trade. [`createLookAtMatrix`](engine/v1/handlers/Render.js) is handed `cameraState.position` raw, with no `toWorldUnit()` conversion, while [`CreateRenderMatrix`](engine/v1/math/Matrix.js#L100) converts positions through `toWorldUnit()` — so the view matrix is built in CNU while model matrices are built in world units. This only agrees because `CNU_SCALE === 1` ([Utilities.js:4](engine/v1/math/Utilities.js#L4)). Same latent assumption in the underwater test, which compares `cameraState.position.y` against `waterLevelCnu` raw. ED independently confirmed the read. The new scatter `cullRadius` is converted correctly on its own terms, but the distance it is compared against is produced by that same view matrix, so if `CNU_SCALE` ever changes the two break together rather than the cull masking the older fault.
- [ ] [2026-08-20] Non-instanced scatter path (`generateObjectScatter`/`BuildObject`) shares the `textures.json` decal singleton across all its meshes instead of cloning it, the same defect just fixed on the instanced batch path — deferred by main agent, because the path is dead code today: no caller ever supplies `scatterContext`, so `sceneGraph.scatter` is permanently empty. Fix by applying the same `cloneScatterDecal`-style clone in `NewScatter.js`'s `canonicalParts` if this path is ever revived.
- [ ] [2026-08-20] WebGL buffer-creation null guards (`ensureDecalQuadBuffer`, `ensureSharedGeometry`, and their 4 downstream consumer guards in `handlers/Render.js`) arguably violate `FORBIDDEN_DEFENSIVE_CHECKS.md` — WebGL context validity is a one-time `Bootup.js` gate, not a per-call renderer concern — deferred by ERA, because it's a pre-existing, engine-wide pattern that should be removed in one dedicated pass at the `Bootup.js` boundary, not patched piecemeal at whichever new call site happens to touch it next.
- [ ] [2026-08-18] Bulk-rename existing authored content off `primary`/`secondary` onto `color`/`texture.color` — deferred by user, because `primary`/`secondary` remain fully supported (deprecated, not removed) after this pass's schema promotion, so no functional need forces the rewrite. ~40+ blocks across [`engine/v1/builder/templates/textures.json`](engine/v1/builder/templates/textures.json), [`engine/v1/builder/templates/characters.json`](engine/v1/builder/templates/characters.json), [`engine/v1/player/characters.json`](engine/v1/player/characters.json) currently author `primary`/`secondary` directly; a future pass can migrate them to the modern names at leisure.
- [ ] [2026-08-05] Translucent terrain and obstacles still depth-reject one another — deferred by main agent, because routing them through the new entity-style translucent pass would break void-wall stencil occlusion. [`drawDepthPrePass`](engine/v1/handlers/Render.js#L1287) primes the void stencil from terrain and obstacles, and relies on full depth-write from that same geometry; making those meshes skip depth-write in the main pass would leave the two paths disagreeing. Terrain and obstacles also draw under `stencilRejectMask` 0x3F/0x40 filters, which a single globally-sorted translucent list cannot preserve. The real fix is teaching the depth-prepass/stencil system to distinguish translucent terrain/obstacles from the entity translucent pass — a dedicated follow-up, because it reaches into the nullSpace/void-wall system. **Updated 2026-09-03: the stated blocker is gone.** Build-time carving deleted the depth pre-pass, the stencil count and the `stencilRejectMask` filters outright, so nothing in the void system depends on terrain depth-write any more. The limitation itself still stands — translucent terrain and obstacles continue to depth-reject one another — but it is now an ordinary translucent-sorting problem with no void coupling, and re-scoping it should start from the current renderer rather than from this entry's reasoning.
- [ ] [2026-08-05] Translucent sorting is per-object, not per-fragment — deferred by main agent, because a correct fix is a different technique entirely (weighted-blended OIT or depth peeling), not an increment on this pass. Three consequences share this root: two intersecting translucent meshes pop when their centroid order flips; a mesh straddling the water surface is bucketed whole by its origin's `y` ([Render.js:1355](engine/v1/handlers/Render.js#L1355)); and parts of one entity that are translucent no longer occlude one another. All are negligible at particle scale and would matter for large intersecting translucent geometry or a heavily self-overlapping translucent model.
- [ ] [2026-08-05] Decals on a translucent entity part now draw underneath it — deferred by main agent, because the fix is a render-pipeline restructure rather than a local change: the decal pass would have to be split so a translucent part's decals draw with the part in PASS D instead of wholesale in PASS E, which means resolving decal ordering against the same depth-write-free sort the parts use. Introduced by this pass. `drawDecalPass` ([Render.js:1463](engine/v1/handlers/Render.js#L1463)) runs at PASS E and walks all entity parts regardless of bucket, drawing with `depthMask(false)`; translucent parts moved from PASS A (before decals) to PASS D (after), so such a part now blends over its own decal. Previously correct in both directions.
- [ ] [2026-08-04] Particle generators on animated entity parts do not follow the animation — deferred by main agent, because fixing it means deciding whether generators should track the display pose or the true pose, a design question outside this pass. [`targetTransform`](engine/v1/handlers/game/Level.js#L422) resolves an entity part to `part.mesh.transform`, which [`applyModelPose`](engine/v1/builder/NewEntity.js#L339) writes from the *true* hierarchy pose; `ResolveEntityAnimation` writes only `displayTransform`/`displayColor` ([Animation.js:153](engine/v1/handlers/game/Animation.js#L153)) alongside it. `mesh.transform` therefore never carries animation, so a generator on a part that swings through one stays put. Engine-wide, not Simulator-specific — merely first visible in the Simulator, the one place an idle model animates in isolation.
- [ ] [2026-08-03] Void-mode carriers cannot hold a particle generator — deferred by ED and main agent, because it needs a design decision rather than a silent behaviour. [NewLevel.js:367-373](engine/v1/builder/NewLevel.js#L367) still sources generators from `allTerrain` and `allObstacleRecords`, which include void objects, but [`resolveGeneratorTargets`](engine/v1/handlers/game/Level.js#L427) only walks `sceneGraph.terrain`/`.obstacles`/`.entities`, which exclude them. A generator authored on a void object therefore orphans on frame one and fades out instead of emitting forever. No such content exists in testGame. Options: add a fourth lookup collection, or warn-and-drop at the boundary the way root placement now does.
- [ ] [2026-08-03] Runtime-spawned entities do not create their authored part generators — deferred by main agent, because only the level-build path sweeps for generators. `SpawnIntoScene` now builds entity part meshes carrying `meta.particle` intact (via [NewEntity.js:191](engine/v1/builder/NewEntity.js#L191)), but nothing turns them into groups, so an entity spawned at runtime with an authored part generator produces nothing and logs nothing.
- [ ] [2026-07-29] Particle seeding budget is spent even when generation is gated out — deferred by ED and main agent, because closing it needs a generation-success signal fed back from the driver into the group, a contract the phase deliberately avoided. `seedDue` increments its counter unconditionally ([NewParticles.js:361](engine/v1/builder/NewParticles.js#L361)) but the sim-distance gate lives inside `GenerateParticles` ([:398](engine/v1/builder/NewParticles.js#L398)), so a sibling due while the camera is out of range is consumed from the `amount` budget and never generated — the plume stays permanently one emission thin even after the player returns. Same class as the accepted permanent build-time skip.
- [ ] [2026-07-23] Super Jeff's face image decals — the smug half-lidded eyes and the off-center smirk — deferred by user, because pass 1 was scoped to "blank face, pure geometry" so silhouette and proportion could be judged before decals land, avoiding re-tuning parts afterward. Neither is authorable with the existing shape decals (`square`/`circle`/`triangle` only, per [canonSchemas.json:1362](engine/v1/core/canonSchemas.json#L1362)) — the angled lids and the smirk both need generated PNGs under a new `engine/v1/player/assets/jeff/`, referenced as `"../assets/jeff/..."` (the player path convention; the entity `"../../engine/v1/assets/..."` form is rejected by `validatePlayerEntry`). The brows are already geometry (tube parts) and are not blocked on this. A chest logo was also considered and dropped for now.
- [ ] [2026-07-23] Boot-time template animation resolution — deferred by user, because the v1 template pass was scoped to composition/repeat/overrides; entity templates in [builder/templates/characters.json](engine/v1/builder/templates/characters.json) ship `animations: {}` and the payload-side resolvers (`resolveAnimations`/`instanceAnimationTracks` in [normalize.js](engine/v1/core/normalize.js)) would need boot-time wiring into [Instance.js](engine/v1/builder/templates/Instance.js).
- [ ] [2026-07-23] Template image decals — deferred by ED, because template data never passes the payload image-preload pipeline (`ctx.pendingImageLoads` in normalize.js), so a template decal would reach the builder without its `bitmap`; templates ship `custom: []` until template-side image loading exists.
- [ ] [2026-07-20] Reconcile the player's swept-solid physics-collision path with regular entities — deferred by user, because it needs its own investigation first. `DetectPhysicsCollisions` (`physics/Collision.js`, player branch) has no tight `AabbOverlap(entity.collision.aabb, candidate.aabb)` gate on solids — it culls only by the 24-padded `simRadiusAabb` then swept-sphere — whereas its sibling `DetectCurrentPhysicsOverlaps` does gate on the tight AABB. That asymmetry let an oversized collider register ground contact through a gap. The inscribed-sphere fix removed the observable symptom; the user wants to understand why the player has a different physics-collision path than entities before deciding how to unify the gating.
- [ ] [2026-07-10] Carl tentacle rework from the capsule chain to the node-chain `tube` primitive — deferred by user, because the payoff is mainly animation quality and it requires tube-specific animation support that does not exist yet; redoing the geometry now would discard the just-tuned capsule geometry and decal work. Revisit when animation work begins.
- [ ] [2026-08-29] Authoring the water meshes' own colours — deferred by MAIN, because `water.tint` was scoped to the underwater camera filter only, and overriding the meshes is a separate feature. `buildWaterVisualMeshes` passes `primary: null, secondary: null` so both take template colours; note `sea-surface` carries two *distinct* colours (highlight and deep), so a single override field would flatten the surface. `ComputeGeneratedTextureID` already folds non-null primary/secondary into the texture id, so dedup would come free.
- [ ] [2026-09-07] `drainPairings` spreads an unbounded array into `push`, which overflows the call stack on a large carve — deferred by MAIN, because it surfaced from a content placement that was itself wrong and the correct move was to stop creating that carve, not to paper over the crash. [`drainPairings`](engine/v1/builder/NewVoid.js) does `relation.openFaces.push(...clipHostSurfaceToVoid(voidSolid, host.solid))`; when `cave-tunnel` was briefly lowered into `complex-geo-ground` the resulting openFace count was large enough that the spread blew the stack and the level failed to build. The fix is a loop or `concat` rather than a spread, but it is worth pairing with a look at *why* a void grazing a large host produces that many fragments in the first place — the same sliver population already noted in the `minimumOpenFaceArea` entry above. No content in lvl2 triggers it today.
