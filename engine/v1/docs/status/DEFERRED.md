# Deferred Work

Explicitly deferred implementations or decisions — work that was started, considered,
or raised during a task and consciously postponed, along with who deferred it and why.

This is NOT the idea backlog (see `engine/v1/docs/todo` for that). Entries here should
only be things that came up mid-task and got pushed off, not general feature ideas.

Entries persist across weekly reports until resolved — remove or check off an item
once it's actually addressed.

## Entry format

`- [ ] [YYYY-MM-DD] Item — deferred by WHO, because REASON`

Example:

`- [ ] [2026-07-09] Camera.js CNU_SCALE calibration — deferred by ED, because it depends on render pipeline stabilizing first (see CNU_VS_WORLDUNITS.md Section 4)`

## Pending

- [ ] [2026-07-23] Super Jeff's face image decals — the smug half-lidded eyes and the off-center smirk — deferred by user, because pass 1 was scoped to "blank face, pure geometry" so silhouette and proportion could be judged before decals land, avoiding re-tuning parts afterward. Neither is authorable with the existing shape decals (`square`/`circle`/`triangle` only, per [canonSchemas.json:1362](engine/v1/core/canonSchemas.json#L1362)) — the angled lids and the smirk both need generated PNGs under a new `engine/v1/player/assets/jeff/`, referenced as `"../assets/jeff/..."` (the player path convention; the entity `"../../engine/v1/assets/..."` form is rejected by `validatePlayerEntry`). The brows are already geometry (tube parts) and are not blocked on this. A chest logo was also considered and dropped for now.
- [ ] [2026-07-22] `instanceBlueprintTemplates()` in [builder/templates/Instance.js](engine/v1/builder/templates/Instance.js) assumes terrain/obstacle blueprints have a `.model.parts` part-shaped structure (dimensions/localPosition/localRotation/pivot) — deferred by main agent, because `terrainBlueprints.json` and `obstacleBlueprints.json` are both `{}` today so the assumption cannot be verified yet. The loop is inert (self-flagged inline). Confirm the assumed shape against the real schema the moment those JSONs are authored.
- [x] [2026-07-21] **RESOLVED [2026-07-22]** — implemented as boot-time instancing in [builder/templates/Instance.js](engine/v1/builder/templates/Instance.js) (see changelog). Landed distributed differently from the note below: a single central module in `builder/templates/` (not `core/InstanceTemplates.js`), no idempotency guard (a double-call is a forbidden defensive check, not a case to guard), and the `toUnitVector3` split from `normalize.js` kept intentional rather than centralized. `ENGINE.Blueprints.*` templates are exposed post-instancing (engine-translated, not authored shape) — deferred by user, because the fix is engine work awaiting a go-ahead, routing through ED (with ERA/DRYAD follow-up). **Scope: all import-time-instanced templates, not just players.** Two import-time in-place instancers exist today and both corrupt their API clones identically: [Model.js:18](engine/v1/player/Model.js#L18)'s IIFE instances `characters.json` → `ENGINE.Blueprints.PlayerCharacters` is radians/`{value,type}` (the direct cause of the Simulator dropping player rotations — see AGENT_LOG 2026-07-21), and [NewScatter.js:19](engine/v1/builder/NewScatter.js#L19)'s IIFE instances `textures.json` `scatterTypes` → `ENGINE.Blueprints.Scatter` is corrupted the same way (not yet observed only because nothing consumes it via the API for authoring). Each IIFE mutates its shared JSON singleton (degrees→radians, →`UnitVector3`/`Unit`) before [ini.js:46-49](engine/v1/core/ini.js#L46-L49) `structuredClone`s it. `terrainBlueprints.json`/`obstacleBlueprints.json` are currently `{}` (empty) so they are *incidentally* raw — **do not scope them out; they will follow the identical pattern once populated** and must be covered by the design now. Safe to defer: the only import-time reads of instanced data are the IIFEs themselves ([NewScatter.js:21-22](engine/v1/builder/NewScatter.js#L21-L22), [Model.js:22](engine/v1/player/Model.js#L22)); all other reads ([NewScatter.js:268](engine/v1/builder/NewScatter.js#L268), [Master.js:80](engine/v1/player/Master.js#L80)) are call-time/post-boot. `toUnitVector3` is duplicated verbatim in [Model.js:19](engine/v1/player/Model.js#L19) and [NewScatter.js:20](engine/v1/builder/NewScatter.js#L20) — DRY evidence for centralizing. **Chosen direction (user-endorsed):** one new module (proposed `core/InstanceTemplates.js`, sibling to `normalize.js`) owning the single `toUnitVector3` helper and one canonicalization routine per template set — characters, scatter, and terrain/obstacle blueprints (the latter two written to the same shape now so they go live the moment those JSONs fill in). It exports **functions, not an IIFE**; `Initialize()` clones raw into the API blueprints first ([ini.js:46-49](engine/v1/core/ini.js#L46-L49)) **then** calls the instancing function(s), so the API returns authored JSON while the shared singletons get instanced for engine use, ordering under the initializer's control. Idempotency guard required (`UNIT_INSTANCING` preserved — only the entry point moves import-time → boot-time). The IIFEs are removed from `Model.js`/`NewScatter.js`, which keep consuming the same singletons, now instanced at boot. Constraint: the Simulator App must keep consuming via the ENGINE API, not a direct `characters.json` import/fetch — the fix belongs at the engine boundary.
- [ ] [2026-07-20] Reconcile the player's swept-solid physics-collision path with regular entities — deferred by user, because it needs its own investigation first. `DetectPhysicsCollisions` (`physics/Collision.js`, player branch) has no tight `AabbOverlap(entity.collision.aabb, candidate.aabb)` gate on solids — it culls only by the 24-padded `simRadiusAabb` then swept-sphere — whereas its sibling `DetectCurrentPhysicsOverlaps` does gate on the tight AABB. That asymmetry let an oversized collider register ground contact through a gap. The inscribed-sphere fix removed the observable symptom; the user wants to understand why the player has a different physics-collision path than entities before deciding how to unify the gating.
- [ ] [2026-07-10] Carl tentacle rework from the capsule chain to the node-chain `tube` primitive — deferred by user, because the payoff is mainly animation quality and it requires tube-specific animation support that does not exist yet; redoing the geometry now would discard the just-tuned capsule geometry and decal work. Revisit when animation work begins.
- [ ] [2026-07-10] Angle-aware, asymmetric connector handle lengths in `SampleConnectorCenterline` — deferred by user, because the current constant symmetric fraction (`chord * 0.667`) is acceptable for now. The correct handle length for a circular arc depends on the turn angle, and an asymmetric corner wants a shorter handle on the end whose tangent sits far off the chord. This would suppress overshoot on asymmetric connectors, but cannot rescue an over-constrained connector whose two node tangents contradict its chord.
- [ ] [2026-07-10] `curved` / `smoothness` on a tube's final node are silently ignored — deferred by user, because it is not currently causing a visible problem. `buildTube` iterates `i < nodes.length - 1` and reads `nodes[i].curved`, so a node owns only the connector that departs it and the last node owns none; authoring `curved: false` on the last node therefore has no effect.
- [ ] [2026-07-10] Decouple the tube connector centerline sample count from the cross-section segment count — deferred by main agent, because it should only be pursued if faceting is actually observed. `buildTube` reuses `resolveCylinderSegments(complexity)` (16 at `high`) both for radial ring resolution and for the number of centerline samples, so a 180-degree U-turn connector is resolved by only ~17 points.
